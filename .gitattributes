# Normalize all text files to LF on commit; never emit CRLF on checkout.
# Prevents Windows<->Linux line-ending churn on this shared checkout.
* text=auto eol=lf

*.sh text eol=lf
.github/workflows/*.yml text eol=lf
.github/workflows/*.yaml text eol=lf
