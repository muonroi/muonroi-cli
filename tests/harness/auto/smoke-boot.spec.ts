/**
 * AUTO-GENERATED by /self-verify — DO NOT hand-edit.
 *
 * Scenario:  smoke-boot
 * Captured:  2026-07-01T07:40:28.938Z
 * Duration:  0ms
 * Derived:   files=[.claude/hooks/self-qa-post-edit.cjs, .claude/settings.json, .muonroi-flow/decisions.md, scripts/ee-ingest-bb-data.js, scripts/ee-seed-behavioral.js, scripts/ee-seed-principles.js, src/__test-stubs__/vitest-setup.ts, src/ee/auth.test.ts, src/ee/auth.ts, src/ee/client.test.ts, src/ee/client.ts, src/ee/scope.test.ts, src/ee/scope.ts, src/flow/compaction/__tests__/extract.test.ts, src/flow/compaction/compress.ts, src/flow/compaction/extract.ts, src/flow/compaction/index.ts, src/orchestrator/council-manager.ts, src/orchestrator/orchestrator.ts, src/orchestrator/scope-reminder.ts, src/orchestrator/session-experience.test.ts, src/orchestrator/session-experience.ts, src/orchestrator/stream-runner.ts, src/pil/discovery-types.ts, src/pil/discovery.ts, src/pil/layer15-context-scan.ts, src/router/decide.test.ts, src/storage/__tests__/migrations.test.ts, src/storage/migrations.ts, src/storage/transcript.ts, src/tools/registry.ts, src/ui/slash/compact.ts, src/ui/use-app-logic.tsx, tests/harness/auto/smoke-boot.spec.ts] semanticIds=[(none)]
 *
 * To rebuild: `muonroi-cli self-verify --since HEAD~1`.
 */
import { afterAll, beforeAll, describe, expect, it } from "vitest";
import { type HarnessContext, spawnHarness } from "../helpers.js";

describe.skipIf(process.platform === "win32" && process.env["MUONROI_SKIP_NAMED_PIPE"] === "1")(
  "self-qa auto: smoke-boot",
  () => {
    let ctx: HarnessContext;

    beforeAll(async () => {
      ctx = await spawnHarness();
      await ctx.driver.wait_for({ idle: true, timeoutMs: 15_000 });
      // 120s: cold agent-mode TUI boot under CPU contention can take 25-46s+
      // (the named-pipe handshake budget alone is 90s). A tighter hook timeout
      // is the wrong constraint and surfaces as a flaky "Hook timed out".
    }, 120_000);

    afterAll(() => {
      ctx?.cleanup();
    });

    it("CLI boots and reaches idle within budget", async () => {
      const { driver } = ctx;
      await driver.wait_for({ idle: true, timeoutMs: 15000 });

      // expect: idle reached within budget (enforced by test timeout)
      // expect: no error toast
      expect(driver.last_event("toast")?.level).not.toBe("error");
    }, 20000);
  },
);
