/**
 * AUTO-GENERATED by /self-verify — DO NOT hand-edit.
 *
 * Scenario:  smoke-boot
 * Captured:  2026-06-25T04:33:24.063Z
 * Duration:  0ms
 * Derived:   files=[packages/agent-harness-react/src/install.ts, packages/agent-harness-react/src/semantic.tsx, src/index.ts, src/orchestrator/auto-commit.ts, src/orchestrator/orchestrator.ts, src/orchestrator/pending-calls.ts, src/orchestrator/preprocessor.ts, src/orchestrator/prompts.ts, src/orchestrator/stream-runner.ts, src/orchestrator/subagent-compactor.spec.ts, src/orchestrator/subagent-compactor.ts, src/orchestrator/tool-engine.ts, src/pil/discovery.ts, src/storage/transcript-view.ts, src/ui/app.tsx, src/ui/containers/chat-feed.tsx, src/ui/use-app-logic.tsx, tests/harness/auto/list-council-card-${idx}.spec.ts, tests/harness/auto/list-council-msg-${idx}.spec.ts, tests/harness/auto/list-council-phases.spec.ts, tests/harness/auto/list-council-status.spec.ts, tests/harness/auto/list-msg-${i}.spec.ts, tests/harness/auto/smoke-boot.spec.ts] semanticIds=[(none)]
 *
 * To rebuild: `muonroi-cli self-verify --since HEAD~1`.
 */
import { afterAll, beforeAll, describe, expect, it } from "vitest";
import { spawnHarness, type HarnessContext } from "../helpers.js";

describe.skipIf(process.platform === "win32" && process.env["MUONROI_SKIP_NAMED_PIPE"] === "1")(
  "self-qa auto: smoke-boot",
  () => {
    let ctx: HarnessContext;

    beforeAll(async () => {
      ctx = await spawnHarness();
      await ctx.driver.wait_for({ idle: true, timeoutMs: 15_000 });
      // 120s: cold agent-mode TUI boot under CPU contention can take 25-46s+
      // (the named-pipe handshake budget alone is 90s). A tighter hook timeout
      // is the wrong constraint and surfaces as a flaky "Hook timed out".
    }, 120_000);

    afterAll(() => {
      ctx?.cleanup();
    });

    it("CLI boots and reaches idle within budget", async () => {
      const { driver } = ctx;
      await driver.wait_for({"idle":true,"timeoutMs":15000});
      
      // expect: idle reached within budget (enforced by test timeout)
      // expect: no error toast
      expect(driver.last_event("toast")?.level).not.toBe("error");
    }, 20000);
  },
);
