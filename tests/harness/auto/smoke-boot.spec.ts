/**
 * AUTO-GENERATED by /self-verify — DO NOT hand-edit.
 *
 * Scenario:  smoke-boot
 * Captured:  2026-06-08T10:19:38.582Z
 * Duration:  490ms
 * Derived:   files=[*.db, .planning/ROADMAP.md, .planning/STATE.md, AGENTS.md, CHANGELOG.md, CLAUDE.md, bun.lock, package.json, scripts/fix-extensions.cjs, scripts/postinstall.cjs, src/agent-harness/index.ts, src/chat/chat-keychain.ts, src/cli/keys.ts, src/cli/usage-report.ts, src/council/__tests__/audit-replay.test.ts, src/ee/types.ts, src/generated/version.ts, src/index.ts, src/maintain/__tests__/codebase-intel.test.ts, src/mcp/ee-tools.ts, src/mcp/mcp-keychain.ts, src/ops/__tests__/doctor-ee-health.test.ts, src/orchestrator/__tests__/read-path-budget.test.ts, src/orchestrator/__tests__/usage-shape-threading.test.ts, src/orchestrator/agent.test.ts, src/orchestrator/compaction.test.ts, src/orchestrator/compaction.ts, src/orchestrator/council-manager.ts, src/orchestrator/message-processor.ts, src/orchestrator/orchestrator.ts, src/orchestrator/read-path-budget.ts, src/orchestrator/sandbox.test.ts, src/orchestrator/scope-reminder.ts, src/orchestrator/stream-runner.ts, src/orchestrator/subagent-compactor.spec.ts, src/orchestrator/subagent-compactor.ts, src/orchestrator/tool-args-hash.ts, src/orchestrator/tool-loop-cap.ts, src/pil/__tests__/layer3-ee-injection.test.ts, src/pil/__tests__/layer3-injected-chunk.test.ts, src/pil/agent-operating-contract.test.ts, src/pil/agent-operating-contract.ts, src/pil/cheap-model-playbook.test.ts, src/pil/cheap-model-playbook.ts, src/pil/cheap-model-workbooks.test.ts, src/pil/cheap-model-workbooks.ts, src/pil/discovery-types.ts, src/pil/discovery.ts, src/pil/layer1-intent.ts, src/pil/layer16-clarity.ts, src/pil/layer3-ee-injection.ts, src/pil/layer6-output.ts, src/pil/pipeline.ts, src/pil/response-tools.ts, src/pil/session-state.ts, src/product-loop/__tests__/ee-extract-wiring.test.ts, src/product-loop/__tests__/verify-failure-threshold.test.ts, src/providers/auth/grok-oauth.ts, src/providers/keychain.ts, src/scaffold/init-new.ts, src/tools/bash.ts, src/tools/registry.ts, src/ui/utils/text.ts, src/ui/utils/tools.ts, src/usage/decision-log.ts, src/utils/permission-mode.ts, tests/harness/auto/probe_static_rules.cjs, tests/harness/auto/probe_t0_seeds.cjs, tests/harness/auto/seed_t0_policies.js, tests/harness/auto/smoke-boot.spec.ts] semanticIds=[(none)]
 *
 * To rebuild: `muonroi-cli self-verify --since HEAD~1`.
 */
import { afterAll, beforeAll, describe, expect, it } from "vitest";
import { type HarnessContext, spawnHarness } from "../helpers.js";

describe.skipIf(process.platform === "win32" && process.env["MUONROI_SKIP_NAMED_PIPE"] === "1")(
  "self-qa auto: smoke-boot",
  () => {
    let ctx: HarnessContext;

    beforeAll(async () => {
      ctx = await spawnHarness();
      await ctx.driver.wait_for({ idle: true, timeoutMs: 15_000 });
      // 120s: cold agent-mode TUI boot under CPU contention can take 25-46s+
      // (the named-pipe handshake budget alone is 90s). A tighter hook timeout
      // is the wrong constraint and surfaces as a flaky "Hook timed out".
    }, 120_000);

    afterAll(() => {
      ctx?.cleanup();
    });

    it("CLI boots and reaches idle within budget", async () => {
      const { driver } = ctx;
      await driver.wait_for({ idle: true, timeoutMs: 15000 });

      // expect: idle reached within budget (enforced by test timeout)
      // expect: no error toast
      expect(driver.last_event("toast")?.level).not.toBe("error");
    }, 20000);
  },
);
