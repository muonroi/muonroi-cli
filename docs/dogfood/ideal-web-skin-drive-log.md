# `/ideal` Drive Log — Web Skin (durability + sharpness eval)

Supervisor (Claude) drives muonroi-cli's own `/ideal` to build the web skin. This log
records what each pipeline stage actually did and rates its **sharpness** — the point
of the exercise is to prove the core is durable under a real, long, multi-stage task
and to find where `/ideal` is dull.

**Rating scale (per stage):** 🟢 sharp (did the right thing, no supervisor rescue) ·
🟡 usable-with-nudge (needed a supervisor correction) · 🔴 dull (wrong/stalled/needed
takeover). Every rating cites concrete evidence (event, snapshot, file, timing).

**Setup**
- Worktree: `D:/sources/Core/muonroi-cli-web` · branch `feat/web-skin-slice1` (off `feat/convene-council-tool` @ 52f9e7ca)
- Provider order: xai → opencode → deepseek (fall through on rate limit only)
- North-star brief: `docs/superpowers/specs/2026-07-14-web-skin-design.md`

---

## Task 1 — Slice 1: walking-skeleton chat over localhost-WS

| Stage | Rating | Evidence / notes |
|---|---|---|
| Boot / route | 🟢 | Cold boot clean, `xai/grok-composer-2.5-fast OK` (OAuth, no key modal). `route-decision` = `{path:council, complexity:medium, forceCouncil:true, runId:mrkoeezk9a29}` — router correctly rated a transport+refactor task as medium, did NOT downgrade to hot-path. |
| Research | 🟢 | `council-msg-0 research:research` is codebase-grounded with real file:line for every claim. Caught the exact traps the design cares about: (1) distinguished UI/orchestrator `StreamChunk` (`type:`, types/index.ts:501) from provider `StreamChunk` (`kind:`, providers/types.ts:16) — "must not be confused"; (2) `runHeadlessPrompt` is the closest backend but ONE-SHOT stdout, not multi-turn WS (index.ts:720); (3) `src/ui/utils/format.ts` builders import `@opentui/core` → NOT browser-safe → web reducer must reimplement a thin fold; (4) verified core-headless invariant currently holds; (5) even found `spikes/react-dom-harness/` react-dom pattern + `ws` dep availability. Heartbeat (Task-4 fix) ticked cleanly ~67s. Sharp, no supervisor rescue. |
| Interview | 🟡 | `/ideal` **skipped** the interview entirely (no askcard-open ever fired) and went research → 3-round debate → scoping. Defensible given the detailed brief, but it never confirmed with the supervisor the one decision the council itself flagged as load-bearing (long-lived vs per-prompt Agent). Acceptable-with-note, not a rescue. |
| Council | 🟢 / 🔴 | **Sharp (🟢):** all three critics (Researcher, Cost-Controller, Skeptic) *independently* converged on the one real hidden risk — `[UNVERIFIED: Agent.processMessage multi-turn contract]`: headless only ever calls it once then `cleanup()`, so a long-lived sidecar reusing one Agent across prompts is unproven ("passes demo 1, fails demo 2"). Skeptic also proposed replacing the human "keep core headless" promise with an automated CI import-graph guard. This is exactly the adversarial depth the council is for. **Defect (🔴):** the `Architect` debate seat died with `[Error: Authentication Fails, Your api key: ****noop is invalid]` — one panel seat was assigned a provider whose key resolves to placeholder "noop". Council stayed resilient (continued to a leader verdict despite the dead seat) — good degrade — but a wasted seat. Leader verdict correctly baselined criteria as Unmet 5/5 (nothing built yet). |
| Council (conductor) | 🟢 | Leader-conductor sharpened the debate round-by-round instead of looping generically: R1 `continue` "Establish concrete evidence"; R2 `extend` "**Does one Agent survive done+cleanup for a second processMessage without rebuild?**" (locked onto the exact crux the critics raised); R3 `stop` 5/5-met converged "Accept multi-turn Agent reuse evidence, lock thin-slice design". Converged in 3/8-budget rounds. Minor UX artifact: round-3 `criteriaMet:5` but directive text still prints "Unmet (5/5)" (baseline vs plan-resolved confusion) — cosmetic. |
| Preflight | 🟢 | Emitted an `askcard` "Approve discussion plan for: …walking skeleton…reuses headless core…WS…without touching OpenTUI" — scope statement was an accurate distillation of the goal. Supervisor approved (`askcard-answered answerText:approve`). |
| Plan / sprint | — | `sprint-plan-committed` fired. Watching for implementation to actually write files in the worktree (impl-wedge risk zone). |
| Sprint-1 planning council | 🟢 / 🔴 | **Sharp (🟢):** the sub-council (panel `deepseek-v4-pro, opencode/kimi-k2.7-code, opencode/glm-5.2`, leader `opencode/glm-5.2`) did NOT just accept the multi-turn-Agent assumption — the Researcher *verified it from code* ("same Agent re-drives processMessage after done without inter-turn cleanup(), incl. `--mock-llm`; tests double-drive `sub-session-realdb.test.ts` turn1→turn2"). Evidence-First, in a council. Synthesis (`council-msg-30`) produced a concrete 3-layer architecture + domain-model table + WS frame contract + CR#1/CR#2 acceptance — implementable. A `Walking Skeleton Product Owner` role-seat enforced scope ("veto any VM field beyond {role,text,toolCalls,toolResults,turnId}"). **Defect (🔴):** recurring dead seats `Architect` + `Sidecar Transport Engineer` = `[Auth Fails, api key: ****noop is invalid]` — council spawns more role-seats than it has authed models; overflow seats get a placeholder "noop" key. Resilient (converged anyway) but wasteful + noisy. |
| Implement | 🔴 | **HALT.** `sprint-runner.ts:318` total-watchdog fired: "isolated implementation stage exceeded **900s** (15 min) total watchdog … the isolated sub-agent turn never completed". Evidence: **zero source files written** in 15 min (git status = only `.planning/` changed); impl ran on `ctx.sessionModelId` = **grok-composer-2.5-fast** (xAI OAuth) because `MUONROI_IDEAL_IMPL_MODEL` was unset. Root cause (high-confidence): grok-composer's OAuth credential did not propagate into the isolated sub-agent context (same "noop"-key failure mode as the dead council seats) → auth-fail/retry → no forward progress → the coarse 15-min blind ceiling (the isolated path has NO per-progress stall guard) killed it. Two real weaknesses exposed: (a) isolated impl silently uses the raw session model with no capability/credential fallback; (b) a dead isolated sub-agent wastes the full 15 min before surfacing. |
| PO fix | ▶ | Halt card offered Resume/Retry/Skip-verify/Abort. Since env/model can't change in a live process, PO fix = restart with `MUONROI_IDEAL_IMPL_MODEL=opencode/glm-5.2` (capable, API-keyed, proven working as this run's council leader; matches the xai→opencode→deepseek order) and resume the run from disk (ROADMAP.md holds the 4-phase plan). Council driver stays grok-composer (it performed well there). Deeper code hardening (forward-progress guard for the isolated path; capability-aware impl-model default) noted as follow-up. |
| Implement retry (opencode, streamed) | 🔴 | Restarted with `MUONROI_IDEAL_IMPL_MODEL=opencode/glm-5.2` + `MUONROI_SPRINT_ISOLATED_IMPL=0` (streamed path for visibility). `/ideal resume` re-entered directly at Sprint-1 Implementation (no re-debate — plan persisted; `sprint-1-plan.md` = 9 concrete steps w/ paths+acceptance, genuinely good). opencode DID make a live LLM call (`llm-done totalChars:61 finishReason:tool-calls`) — so opencode is reachable/working — but the impl turn then **wedged after that first tool-call response**: same `llm-done` for 3+ min, zero new LLM activity, zero files, no toast. **Reproduced the wedge on a 2nd provider + the streamed path.** Conclusion: the wedge is in the impl-turn tool-execution loop (hangs after `finishReason:tool-calls`), not a single-model fluke. Grok wedged too (isolated, 15-min ceiling). Next test per your provider order: deepseek-v4-pro (standard provider, no OAuth/proxy quirks) to isolate provider-specific vs loop bug. |
| Implement retry (deepseek, streamed) | 🔴 | `MUONROI_IDEAL_IMPL_MODEL=deepseek-v4-pro` — deepseek ALSO wedged identically: one `llm-done totalChars:67 finishReason:tool-calls` then silent, zero files. **3/3 providers + 2/2 paths wedge** → not model-specific. |
| ROOT CAUSE (confirmed) | 🔴 core bug | The impl turn's first mutation (Write of a new `src/web/*` file) needs approval in **safe** permission mode. `tool-engine.ts:2997` auto-approves only when `!toolNeedsApproval(...)`; otherwise it surfaces an approval request and `await`s `respondToToolApproval`. In the driven product-loop/agent-mode context **no responder answers and no approval askcard renders** (only the preflight card surfaces) → the await hangs forever → the impl turn wedges after `finishReason:tool-calls`, deterministically, on every provider. Two defects: (a) my setup booted in `safe`; (b) real core bug — autonomous `/ideal` (plan already approved at preflight) should auto-approve its own impl mutations or surface an answerable card, not silently hang. Blocker to a live yolo workaround: the harness argv allowlist (`mcp-server.ts:58`) rejects `--permission`, and the TUI has no in-app permission toggle keybinding (mode is startup-only). |
| PO fix #1 (permission elevation) | 🔴 did NOT resolve | Applied per PO decision: `orchestrator.ts` — the product-loop `processMessageFn` + `runIsolatedTask` now elevate `safe→auto-edit` for the turn's lifetime (restored in `finally`), so autonomous impl file-ops auto-approve. Verified the code loads (harness spawns from `process.cwd()/src/index.ts` = main repo; `_buildMessageProcessorDeps` uses live getters, so per-turn elevation reaches tool-engine). **Retest (deepseek, streamed): still wedged identically** — one `llm-done totalChars:67 finishReason:tool-calls`, then 2+ min silent, 0 files. **Honest conclusion: permission-approval-await was NOT the (sole) root cause.** The impl turn's first tool call is most likely a `read_file` (already auto-approved even in `safe`), so the hang is downstream of approval — in the tool-execution / loop-continuation after the first `finishReason:tool-calls`. Needs instrumented debugging (the black-box harness poke can't see the sub-turn's tool call or exact hang point). |
| TRUE ROOT CAUSE (wire-log) | 🟢 found & fixed | `MUONROI_DEBUG_LLM_WIRE` capture (`.muonroi-flow/llm-wire.log`) showed the real error: xAI `400 invalid-argument: "expected JSON object for tool arguments"`. **grok-composer emits BATCHES of parallel tool_calls (3-4/assistant turn) and some carry a non-object `arguments` value** (bare string/number/array/null — valid JSON, wrong shape). `xai.strategy.ts` had **no `transformRequestBody`** (unlike `opencode-go`/`zai`), so the malformed history reached xAI → 400 → non-transient → the bounded stream-retry re-sent the same bad history → wedge. Also learned: the **streamed impl path ignores `MUONROI_IDEAL_IMPL_MODEL`** (only the isolated path honors it), so every "deepseek/opencode" retest was actually still grok-composer — same model, same bug. **Two-part fix (both scoped, mirror the proven opencode-go pattern):** (1) `xai.strategy.ts` — add `transformRequestBody` applying `splitParallelToolCalls` + `sanitizeToolCallArguments` + drop-null-response_format; (2) `thinking-mode.ts sanitizeToolCallArguments` — extend to repair a string that parses to a **non-object** (was only fixing empty/parse-fail). **Verified live:** after the fix, `splitParallelToolCalls` ran (assistantLens toolCalls all 1, was 3/4/4) AND `expected JSON object` errors dropped to **0**; impl turn now progresses (only a 1-off transient OAuth blip remains, which retries). |
| Implement (post-fix) | 🟢 | grok-composer, now unblocked, self-built the full slice from its own plan: **13 files** — `src/web/sidecar/server.ts` (122L Bun WS sidecar), `transport/{frames,adapter}.ts`, `client/{App.tsx,chat-reducer.ts (110L),main.tsx,index.html}`, `tests/web/{chat-reducer.test,capture-mock-llm,bundle-guard}` + fixture, `.github/workflows/madge.yml` + `scripts/check-web-imports.mjs`, and wired `package.json` + `src/index.ts`. Multi-step LLM loop healthy (many `llm-done`, final `finishReason:stop` 2870-char summary), 0 arg-shape errors throughout. |
| Supervisor (PO) code review | 🟢 | Read `sidecar/server.ts`: faithful to the council's 3-layer architecture + frame contract — long-lived `Agent` reuse, `for await processMessage → sanitizeChunkForWire → WS`, busy-guard, ping/pong, `/health`, SIGINT→`cleanup()` (cleanup only on shutdown, per debate), error frames (No-Silent-Catch). Real senior code, not stubs. Sprint was finalizing/verifying on slow grok-composer at report time. |
| Verify | ▶ | Sprint verify pending (grok-composer slow finalize); code on disk is complete. |

### Timeline
- _(entries added live as the drive proceeds)_
