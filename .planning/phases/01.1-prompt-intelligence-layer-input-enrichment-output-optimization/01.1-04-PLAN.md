---
phase: 01.1-prompt-intelligence-layer-input-enrichment-output-optimization
plan: 04
type: execute
wave: 3
depends_on:
  - 01.1-01
  - 01.1-02
  - 01.1-03
files_modified:
  - tests/arch/no-network-in-pil-layer1.test.ts
autonomous: false
requirements:
  - PIL-01
  - PIL-02

must_haves:
  truths:
    - "Arch guard CI test fails any PR that adds a network import to src/pil/layer1-intent.ts"
    - "Full test suite exits 0 after all Phase 01.1 plans are complete"
    - "All 8 PIL requirements are verified green (unit tests + arch test)"
  artifacts:
    - path: "tests/arch/no-network-in-pil-layer1.test.ts"
      provides: "Arch guard preventing network imports in Layer 1 hot path"
      contains: "src/pil/layer1-intent"
  key_links:
    - from: "tests/arch/no-network-in-pil-layer1.test.ts"
      to: "src/pil/layer1-intent.ts"
      via: "static import analysis checking FORBIDDEN patterns"
      pattern: "no-network-in-pil"
---

<objective>
Ship the arch guard test for PIL Layer 1 (mirrors the existing no-network-in-classifier test), then run the full Phase 01.1 validation suite to confirm all 8 requirements are verified.

Purpose: Layer 1 must stay network-free forever. The arch test enforces this as a CI gate so future developers cannot accidentally add EE/HTTP imports to the hot path. Also serves as the Phase 01.1 completion gate.

Output: Arch guard test committed. Full suite green. Phase 01.1 ready for /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.1-prompt-intelligence-layer-input-enrichment-output-optimization/01.1-CONTEXT.md
@.planning/phases/01.1-prompt-intelligence-layer-input-enrichment-output-optimization/01.1-01-SUMMARY.md
@.planning/phases/01.1-prompt-intelligence-layer-input-enrichment-output-optimization/01.1-02-SUMMARY.md
@.planning/phases/01.1-prompt-intelligence-layer-input-enrichment-output-optimization/01.1-03-SUMMARY.md
</context>

<interfaces>
<!-- Arch test pattern to replicate. Read the existing test before creating the new one. -->

From tests/arch/no-network-in-classifier.test.ts (existing — read full file):
```typescript
// Pattern: read file contents, check for FORBIDDEN import patterns using regex
// FORBIDDEN: node:net | node:http | node:https | undici | axios | ../ee/ | global-fetch

// The test reads the source file(s) and asserts the forbidden strings are not present.
// It does NOT compile or run the code — it's a static analysis check.
```

PIL Layer 1 FORBIDDEN imports (from RESEARCH.md Pitfall 6):
- `node:net`
- `node:http`
- `node:https`
- `undici`
- `axios`
- `../ee/` (EE client must not appear in layer1-intent.ts)
- `fetch` (global fetch)
- Any pattern matching `/http/i` in import paths
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Arch guard for PIL Layer 1 hot path</name>
  <files>
    tests/arch/no-network-in-pil-layer1.test.ts
  </files>
  <read_first>
    - tests/arch/no-network-in-classifier.test.ts (read FULL file — replicate exact pattern)
    - src/pil/layer1-intent.ts (read actual file — verify no forbidden imports before writing test)
    - src/pil/types.ts (read actual file — confirm no network imports in types either)
  </read_first>
  <behavior>
    Arch tests:
    - Test 1: src/pil/layer1-intent.ts does NOT import from 'node:net'
    - Test 2: src/pil/layer1-intent.ts does NOT import from 'node:http' or 'node:https'
    - Test 3: src/pil/layer1-intent.ts does NOT import from 'undici' or 'axios'
    - Test 4: src/pil/layer1-intent.ts does NOT import from '../ee/' or '../../ee/'
    - Test 5: src/pil/layer1-intent.ts does NOT use global fetch (no 'fetch(' calls)
    - Test 6: src/pil/types.ts does NOT import any of the FORBIDDEN patterns (types must stay clean too)
  </behavior>
  <action>
    Create `tests/arch/no-network-in-pil-layer1.test.ts` following the SAME pattern as the existing `no-network-in-classifier.test.ts`.

    Read the existing test first to get the exact file-reading and assertion pattern. Then replicate it for:
    1. `src/pil/layer1-intent.ts` — the hot-path file
    2. `src/pil/types.ts` — the types file (should be trivially clean)

    FORBIDDEN patterns to check (use `includes()` or regex):
    - `'node:net'`
    - `'node:http'`
    - `'node:https'`
    - `'undici'`
    - `'axios'`
    - `from '../ee/` (relative EE import)
    - `from '../../ee/` (two-level relative EE import)
    - `fetch(` (global fetch usage)

    The test should:
    1. Read the file contents using `readFileSync` or Bun's `Bun.file().text()`
    2. Assert each FORBIDDEN pattern is NOT present in the file contents
    3. Be deterministic — same result on every run

    Add a comment at the top: `// Arch guard: Layer 1 hot path must never import network modules.`
    `// Mirrors tests/arch/no-network-in-classifier.test.ts for src/router/classifier/`
  </action>
  <verify>
    <automated>bunx vitest run tests/arch/no-network-in-pil-layer1.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - no-network-in-pil-layer1.test.ts: 6 tests green
    - `bunx vitest run tests/arch` exits 0 (both arch tests pass: classifier + pil-layer1)
    - The test reads src/pil/layer1-intent.ts and src/pil/types.ts (not hardcoded strings)
  </acceptance_criteria>
  <done>
    Arch guard test created and green. Layer 1 hot path confirmed network-free as of this phase.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 01.1 human verification gate</name>
  <action>Run all verification steps in how-to-verify. Confirm automated tests pass before requesting human approval.</action>
  <what-built>
    Full Phase 01.1 implementation:
    - src/pil/ module with 6-layer pipeline (Layer 1 real, Layers 2-5 stubs, Layer 6 real)
    - orchestrator.ts wired: runPipeline() intercepts prompts, applyPilSuffix() injects output rules
    - /optimize slash command registered and working
    - DB migration v3 adds pil_active + enrichment_delta to usage_events
    - Arch guard test for PIL Layer 1 hot path
  </what-built>
  <how-to-verify>
    **Step 1 — Full test suite:**
    ```bash
    bunx vitest run
    ```
    Expected: exits 0, all tests green.

    **Step 2 — TypeScript check:**
    ```bash
    bunx tsc --noEmit
    ```
    Expected: exits 0.

    **Step 3 — Smoke test /optimize (if Anthropic API key available):**
    ```bash
    bun run dev
    ```
    Send any prompt (e.g. "refactor this function"), then type `/optimize`.
    Expected: Layer breakdown table appears with 6 rows, all layer names visible.

    **Step 4 — Verify /optimize with custom arg (headless-safe):**
    ```bash
    bun run dev --prompt "/optimize refactor this legacy function" --format json
    ```
    Expected: JSON output contains layer breakdown table text.

    **Step 5 — Verify fail-open:**
    If Ollama is not running: send any coding prompt. Verify the prompt still reaches the model (not blocked).

    **Step 6 — Check PIL token tracking in /cost:**
    After sending a coding prompt, type `/cost`.
    Expected: response displays (PIL fields optional in v1 — pil_active/enrichment_delta tracked in DB but not required to appear in /cost output yet).
  </how-to-verify>
  <resume-signal>Type "approved" if all automated tests pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
Full Phase 01.1 gate:

```bash
bunx vitest run
```
Expected: exits 0.

```bash
bunx tsc --noEmit
```
Expected: exits 0.

PIL-specific tests:
```bash
bunx vitest run src/pil src/ui/slash/__tests__/optimize.test.ts src/storage/__tests__/migrations.test.ts tests/arch/no-network-in-pil-layer1.test.ts
```
Expected: all green.

Requirement coverage check:
- PIL-01: `bunx vitest run src/pil/__tests__/pipeline.test.ts` — PipelineContext with 6 layers
- PIL-02: `bunx vitest run src/pil/__tests__/layer1-intent.test.ts` — TaskType mapping
- PIL-03: `bunx vitest run src/pil/__tests__/pipeline.test.ts` — stubs applied=false
- PIL-04: `bunx vitest run src/pil/__tests__/layer6-output.test.ts` — suffix conditional
- PIL-05: `bunx vitest run src/pil/__tests__/pipeline.test.ts` — fail-open + timeout
- PIL-06: `bunx vitest run src/storage/__tests__/migrations.test.ts` — migration v3
- PIL-07: `bunx vitest run src/ui/slash/__tests__/optimize.test.ts` — /optimize command
- PIL-08: arch test + store test — getPilLastResult() populated
</verification>

<success_criteria>
- `bunx vitest run` exits 0 (100% green, no flaky tests)
- `bunx tsc --noEmit` exits 0
- tests/arch/no-network-in-pil-layer1.test.ts: 6 tests green
- All 8 PIL requirements have at least one automated test verifying them
- Human checkpoint approved (visual verification of /optimize output in TUI)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-prompt-intelligence-layer-input-enrichment-output-optimization/01.1-04-SUMMARY.md`
</output>
