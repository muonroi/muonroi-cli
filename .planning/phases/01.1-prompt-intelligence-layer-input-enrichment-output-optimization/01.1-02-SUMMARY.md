---
phase: 01.1-prompt-intelligence-layer-input-enrichment-output-optimization
plan: "02"
subsystem: orchestrator/pil
tags: [pil, orchestrator, integration, enrichment, system-prompt]
dependency_graph:
  requires:
    - 01.1-01 (PIL module foundation — runPipeline, applyPilSuffix, PipelineContext)
  provides:
    - PIL pipeline active on every user prompt turn
    - System prompt enriched with OUTPUT RULES suffix for coding tasks
  affects:
    - src/orchestrator/orchestrator.ts (prompt mutation point, system prompt assembly)
tech_stack:
  added: []
  patterns:
    - Fail-open async intercept via .catch() fallback
    - Single-call buildSystemPrompt wrapped by applyPilSuffix before applyModelConstraints
key_files:
  created:
    - src/pil/__tests__/orchestrator-integration.test.ts
  modified:
    - src/orchestrator/orchestrator.ts
decisions:
  - PIL intercept placed after consumeBackgroundNotifications() and before messages.push() — exact mutation point per D-01
  - buildSystemPrompt() called exactly once, result passed to applyPilSuffix(), then applyModelConstraints() — avoids double-call anti-pattern
  - .catch() on runPipeline() is outer fail-open guard; inner 200ms Promise.race is in runPipeline itself (D-04 two-level safety)
metrics:
  duration: 15
  completed_date: "2026-04-30"
  tasks_completed: 2
  files_modified: 3
---

# Phase 01.1 Plan 02: PIL Orchestrator Integration Summary

**One-liner:** PIL runPipeline intercept wired in orchestrator.ts — enriched prompts flow to model, OUTPUT RULES suffix injected for coding tasks via applyPilSuffix wrapping buildSystemPrompt.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Wire runPipeline intercept + system prompt suffix | 3e72dc3 | orchestrator.ts, layer1-intent.test.ts (bug fix) |
| 2 | Orchestrator integration test + full suite green | bdc38f2 | orchestrator-integration.test.ts |

## What Was Built

### Task 1: orchestrator.ts Integration

Two surgical edits to `src/orchestrator/orchestrator.ts`:

1. **Import added** (line 82): `import { runPipeline, applyPilSuffix } from '../pil/index.js'`

2. **Intercept block** replaces the original `messages.push` + `buildSystemPrompt` section:
   - `runPipeline(userMessage)` called after `consumeBackgroundNotifications()` with `.catch()` fail-open
   - `ModelMessage.content` now uses `pilCtx.enriched` (not raw `userMessage`)
   - `buildSystemPrompt()` wrapped by `applyPilSuffix(…, pilCtx)` before `applyModelConstraints()`

### Task 2: Integration Tests

Created `src/pil/__tests__/orchestrator-integration.test.ts` with 5 tests:
- enriched message used when pipeline succeeds
- applyPilSuffix appends OUTPUT RULES for non-null taskType
- applyPilSuffix skips suffix for null taskType
- catch fallback uses raw userMessage (fail-open contract verification)
- getPilLastResult returns same ctx reference after runPipeline

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed pre-existing TypeScript error in layer1-intent.test.ts**
- **Found during:** Task 1 (tsc verification)
- **Issue:** `mockClassify.mockReturnValue({ tier: 'warm', … })` — `ClassifierResult.tier` type is `'hot' | 'abstain'`, `'warm'` is invalid
- **Fix:** Changed `tier: 'warm'` to `tier: 'hot'` (tree-sitter results return `'hot'` per classifier/index.ts logic)
- **Files modified:** `src/pil/__tests__/layer1-intent.test.ts`
- **Commit:** 3e72dc3 (included in Task 1 commit)

## Verification Results

```
bunx tsc --noEmit        → exit 0 (clean)
bunx vitest run src/pil  → 5 files, 25 tests passed
bunx vitest run          → 104 files passed, 5 skipped — 548 tests passed, 6 skipped
```

## Acceptance Criteria Check

- [x] `bunx tsc --noEmit` exits 0
- [x] `grep -n "runPipeline" orchestrator.ts` — 2 matches (import line + call site)
- [x] `grep -n "applyPilSuffix" orchestrator.ts` — 2 matches (import line + wrapping call)
- [x] `grep -n "content: enrichedMessage" orchestrator.ts` — 1 match
- [x] `grep -n "content: userMessage" orchestrator.ts` — 0 matches
- [x] `bunx vitest run` exits 0 (full suite green)
- [x] orchestrator-integration.test.ts has 5 passing tests (> 3 minimum)

## Known Stubs

None — PIL pipeline layers 2-5 are intentional stubs per Phase 01.1 architecture constraint (enforced by arch tests). These are tracked from Plan 01 and will be wired in future phases.

## Self-Check: PASSED
