# Phase 01.1: Prompt Intelligence Layer — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-30
**Phase:** 01.1-prompt-intelligence-layer-input-enrichment-output-optimization
**Areas discussed:** Pipeline Integration, Layer 1 Intent Detection, Layer 6 Output Optimization, /optimize Slash Command
**Mode:** Auto (--auto flag — recommended defaults selected)

---

## Pipeline Integration & Module Structure

| Option | Description | Selected |
|--------|-------------|----------|
| After UserPromptSubmit, before messages.push | Clean in-flow intercept; orchestrator.ts line 1993–1996 | ✓ |
| New pre-streamText middleware | Intercepts later, after system prompt build | |
| Inside fireHook return value | Would require hook API changes | |

**User's choice:** Auto-selected (recommended default)
**Notes:** `SlashContext.lastPrompt?: string` already typed in registry — orchestrator must populate it post-PIL run.

---

## Layer 1 — Intent Detection

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse src/router/classifier/ | Already <1ms hot-path; extend with PIL task types | ✓ |
| New Ollama-based classifier | Higher accuracy, higher latency | |
| Regex-only (no classifier reuse) | Simpler, but duplicates classifier logic | |

**User's choice:** Auto-selected (recommended default)
**Notes:** 6 task types: refactor, debug, plan, analyze, documentation, generate. null = conversational passthrough.

---

## Layer 6 — Output Optimization

| Option | Description | Selected |
|--------|-------------|----------|
| System prompt suffix (task-conditioned) | Appended to buildSystemPrompt() output; no AI SDK changes | ✓ |
| AI SDK toolChoice: "auto" only | Partial enforcement; no conciseness rules | |
| Both suffix + toolChoice hint | More aggressive; potential for model confusion | |

**User's choice:** Auto-selected (recommended default)
**Notes:** Forbidden patterns (no "I cannot", no apology loops) added to suffix. Complex adversarial safety deferred Phase 4.

---

## `/optimize` Slash Command

| Option | Description | Selected |
|--------|-------------|----------|
| src/ui/slash/optimize.ts + registerSlash | Co-located with cost.ts, route.ts — same pattern | ✓ |
| src/pil/slash.ts (co-located with PIL) | Separates UI concern from PIL module | |
| Extend /route command | Wrong semantic home | |

**User's choice:** Auto-selected (recommended default)
**Notes:** /optimize with no args shows last turn. /optimize "prompt" dry-runs pipeline on custom input.

---

## Claude's Discretion

- Exact system prompt suffix wording
- Whether to cache classifier result across turns
- Internal naming of LayerResult fields
- Test fixture strategy

## Deferred Ideas

- Layers 2–5 full implementation (Personality, EE Injection, GSD Structuring, Context Enrichment)
- Ollama naturalization of JSON output
- Rich TUI rendering (diff previews, confirm buttons)
- Remote model-aware output format tuning
