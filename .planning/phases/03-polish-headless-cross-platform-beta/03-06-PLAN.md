---
phase: 03-polish-headless-cross-platform-beta
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__test-stubs__/ee-server.ts
  - src/ee/intercept.test.ts
  - src/ee/touch.test.ts
  - src/router/warm.test.ts
  - src/router/health.test.ts
  - src/router/decide.test.ts
  - src/router/cold.test.ts
  - src/ui/slash/route.test.ts
autonomous: true
gap_closure: true
requirements: [CORE-05]
must_haves:
  truths:
    - "bunx tsc --noEmit exits 0 with no rootDir errors"
    - "All existing tests that import ee-server still pass"
  artifacts:
    - path: "src/__test-stubs__/ee-server.ts"
      provides: "EE stub server relocated inside rootDir"
    - path: "tests/stubs/ee-server.ts"
      provides: "DELETED — no longer exists"
  key_links:
    - from: "src/ee/intercept.test.ts"
      to: "src/__test-stubs__/ee-server.ts"
      via: "relative import"
      pattern: "from.*__test-stubs__/ee-server"
    - from: "src/router/decide.test.ts"
      to: "src/__test-stubs__/ee-server.ts"
      via: "relative import"
      pattern: "from.*__test-stubs__/ee-server"
---

<objective>
Fix pre-existing tsconfig rootDir error that blocks CI matrix typecheck step.

Purpose: `tsconfig.json` sets `rootDir: "./src"` but 7 test files inside `src/` import from `tests/stubs/ee-server.ts` (outside rootDir). This makes `bunx tsc --noEmit` fail, which will break the `Typecheck` step in `ci-matrix.yml` on all 3 OS runners (CORE-05). The fix is to move the stub into `src/` and update all imports.

Output: `src/__test-stubs__/ee-server.ts` with updated imports in all 7 consuming test files. `tests/stubs/ee-server.ts` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ee-server stub into src/__test-stubs__/ and update all imports</name>
  <files>
    src/__test-stubs__/ee-server.ts,
    src/ee/intercept.test.ts,
    src/ee/touch.test.ts,
    src/router/warm.test.ts,
    src/router/health.test.ts,
    src/router/decide.test.ts,
    src/router/cold.test.ts,
    src/ui/slash/route.test.ts
  </files>
  <read_first>
    tests/stubs/ee-server.ts,
    src/ee/intercept.test.ts,
    src/ee/touch.test.ts,
    src/router/warm.test.ts,
    src/router/health.test.ts,
    src/router/decide.test.ts,
    src/router/cold.test.ts,
    src/ui/slash/route.test.ts,
    tsconfig.json
  </read_first>
  <action>
1. Create directory `src/__test-stubs__/`.
2. Copy `tests/stubs/ee-server.ts` to `src/__test-stubs__/ee-server.ts`. Keep the file content identical — only the location changes.
3. Update import paths in all 7 consuming test files:
   - `src/ee/intercept.test.ts`: change `from "../../tests/stubs/ee-server.js"` to `from "../__test-stubs__/ee-server.js"`
   - `src/ee/touch.test.ts`: change `from "../../tests/stubs/ee-server.js"` to `from "../__test-stubs__/ee-server.js"`
   - `src/router/warm.test.ts`: change `from '../../tests/stubs/ee-server.js'` to `from '../__test-stubs__/ee-server.js'`
   - `src/router/health.test.ts`: change `from '../../tests/stubs/ee-server.js'` to `from '../__test-stubs__/ee-server.js'`
   - `src/router/decide.test.ts`: change `from '../../tests/stubs/ee-server.js'` to `from '../__test-stubs__/ee-server.js'`
   - `src/router/cold.test.ts`: change `from '../../tests/stubs/ee-server.js'` to `from '../__test-stubs__/ee-server.js'`
   - `src/ui/slash/route.test.ts`: change `from '../../../tests/stubs/ee-server.js'` to `from '../../__test-stubs__/ee-server.js'`
4. Delete `tests/stubs/ee-server.ts`. If `tests/stubs/` directory is now empty, delete the directory too.
5. Check if any other files under `tests/` are imported by `src/` files. If found, move them too. If `tests/` dir has other test files that do NOT get imported by `src/`, leave them.

IMPORTANT: Use `.js` extension in import paths (not `.ts`) — this is the existing project convention for TypeScript imports with `moduleResolution: "Bundler"`.
  </action>
  <verify>
    <automated>cd D:/Personal/Core/muonroi-cli && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits 0 with zero errors
    - `grep -r "tests/stubs/ee-server" src/` returns no matches
    - `src/__test-stubs__/ee-server.ts` exists and exports `startStubEEServer` and `StubHandle`
  </acceptance_criteria>
  <done>
    - bunx tsc --noEmit exits 0
    - All 7 test files import from src/__test-stubs__/ee-server.js
    - tests/stubs/ee-server.ts no longer exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all existing tests still pass after the move</name>
  <files>none (verification only)</files>
  <read_first>none</read_first>
  <action>
Run the full test suite to confirm nothing broke from the import path changes. Specifically run:
1. `bunx vitest run src/ee/intercept.test.ts src/ee/touch.test.ts` — EE tests
2. `bunx vitest run src/router/warm.test.ts src/router/health.test.ts src/router/decide.test.ts src/router/cold.test.ts` — Router tests
3. `bunx vitest run src/ui/slash/route.test.ts` — Slash command test
4. `bunx vitest run` — Full suite to catch any transitive breakage

If any test fails due to the import move, fix the import path. The test logic itself must NOT change.
  </action>
  <verify>
    <automated>cd D:/Personal/Core/muonroi-cli && bunx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run` exits 0
    - Zero test failures
    - Output contains "passed" count matching pre-move count (no tests dropped)
  </acceptance_criteria>
  <done>
    - Full test suite passes with 0 failures
    - bunx tsc --noEmit exits 0
    - CI matrix typecheck step will no longer fail on rootDir error
  </done>
</task>

</tasks>

<verification>
1. `bunx tsc --noEmit` exits 0 (the primary gap this plan closes)
2. `bunx vitest run` exits 0 (no regression)
3. `grep -r "tests/stubs/ee-server" src/` returns empty (all imports updated)
4. `test -f src/__test-stubs__/ee-server.ts` succeeds (stub relocated)
5. `test ! -f tests/stubs/ee-server.ts` succeeds (old location removed)
</verification>

<success_criteria>
- TypeScript typecheck passes with zero errors
- Full test suite green
- CI matrix Typecheck step will pass on all 3 OS runners
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-headless-cross-platform-beta/03-06-SUMMARY.md`
</output>
