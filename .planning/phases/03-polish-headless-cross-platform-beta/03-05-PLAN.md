---
phase: 03-polish-headless-cross-platform-beta
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - STATUS.md
autonomous: true
requirements: [OPS-03, OPS-04]

must_haves:
  truths:
    - "User finds GitHub issue templates when creating a new issue"
    - "Bug report template requires muonroi-cli doctor output"
    - "Bug report template includes auto-redaction guidance"
    - "STATUS.md exists at repo root with known issues, beta enrollment, and rollout plan"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug report template with doctor output requirement"
      contains: "doctor"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Feature request template"
      contains: "feature"
    - path: "STATUS.md"
      provides: "Beta status page with known issues and enrollment"
      contains: "Beta"
  key_links:
    - from: ".github/ISSUE_TEMPLATE/bug_report.yml"
      to: "src/ops/doctor.ts"
      via: "Template instructs user to paste doctor output"
      pattern: "muonroi-cli doctor"
    - from: ".github/ISSUE_TEMPLATE/bug_report.yml"
      to: "src/ops/bug-report.ts"
      via: "Template instructs user to attach bug-report output"
      pattern: "muonroi-cli bug-report"
---

<objective>
Create GitHub issue templates (bug report with auto-redaction guidance + feature request) and STATUS.md with known issues, beta enrollment instructions, and gradient rollout plan.

Purpose: OPS-03 and OPS-04 provide the solo-maintainer beta support surface. Issue templates standardize reports; STATUS.md communicates beta state to users.
Output: Two GitHub issue template YAML files and STATUS.md at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub issue templates</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml</files>
  <read_first>.github/workflows/ci-matrix.yml</read_first>
  <action>
Create `.github/ISSUE_TEMPLATE/bug_report.yml`:

```yaml
name: Bug Report
description: Report a bug in muonroi-cli
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        ## Before submitting

        1. Run `muonroi-cli doctor` and paste the output below.
        2. Run `muonroi-cli bug-report > report.json` and attach the file.
        3. **IMPORTANT: Auto-redaction** — `bug-report` automatically scrubs API keys and secrets.
           If you include any other output (logs, terminal screenshots), manually redact:
           - API keys (anything starting with `sk-`)
           - Auth tokens
           - File paths containing usernames (replace with `~`)
           - Any prompt content you consider private

  - type: textarea
    id: doctor-output
    attributes:
      label: Doctor Output
      description: Paste the output of `muonroi-cli doctor`
      placeholder: |
        [PASS] bun_version: Bun 1.3.13
        [PASS] os: win32 10.0.26200 x64
        ...
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Bug Description
      description: What happened? What did you expect to happen?
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to Reproduce
      description: Minimal steps to reproduce the bug
      placeholder: |
        1. Run `muonroi-cli --prompt "..."`
        2. ...
    validations:
      required: true

  - type: dropdown
    id: permission-mode
    attributes:
      label: Permission Mode
      description: Which permission mode were you using?
      options:
        - safe (default)
        - auto-edit
        - yolo
    validations:
      required: true

  - type: textarea
    id: bug-report-json
    attributes:
      label: Bug Report Bundle
      description: "Attach or paste the output of `muonroi-cli bug-report` (auto-redacted)"
      render: json
    validations:
      required: false
```

Create `.github/ISSUE_TEMPLATE/feature_request.yml`:

```yaml
name: Feature Request
description: Suggest a feature for muonroi-cli
title: "[Feature]: "
labels: ["enhancement"]
body:
  - type: textarea
    id: problem
    attributes:
      label: Problem
      description: What problem does this feature solve?
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: Proposed Solution
      description: How would you like this to work?
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives Considered
      description: Any other approaches you considered?
    validations:
      required: false

  - type: dropdown
    id: scope
    attributes:
      label: Scope
      description: Which area does this affect?
      options:
        - CLI / TUI
        - Provider / Model
        - Router / Cap
        - Experience Engine
        - Flow / Slash Commands
        - MCP / LSP / Tools
        - Headless / CI
        - Other
    validations:
      required: true
```
  </action>
  <verify>
    <automated>cd D:/Personal/Core/muonroi-cli && test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .github/ISSUE_TEMPLATE/bug_report.yml exists and contains `muonroi-cli doctor`
    - .github/ISSUE_TEMPLATE/bug_report.yml contains `muonroi-cli bug-report`
    - .github/ISSUE_TEMPLATE/bug_report.yml contains `sk-` in redaction guidance
    - .github/ISSUE_TEMPLATE/bug_report.yml contains `validations:` with `required: true` for doctor output
    - .github/ISSUE_TEMPLATE/feature_request.yml exists and contains `Feature Request`
    - Both files are valid YAML (no syntax errors)
  </acceptance_criteria>
  <done>Bug report template requires doctor output, references bug-report command, includes auto-redaction guidance. Feature request template has structured fields for problem/solution/scope.</done>
</task>

<task type="auto">
  <name>Task 2: Create STATUS.md</name>
  <files>STATUS.md</files>
  <read_first>.planning/ROADMAP.md, .planning/PROJECT.md</read_first>
  <action>
Create `STATUS.md` at repo root with the following content:

```markdown
# muonroi-cli Status

**Current Version:** v0.1.0-beta
**Stage:** Private Beta
**Last Updated:** 2026-04-30

## Current State

muonroi-cli is in **private beta**. The CLI is functional for daily use with the following capabilities:

- Multi-provider support (Anthropic, OpenAI, Gemini, DeepSeek, Ollama) with BYOK
- 3-tier brain router (local classifier, warm EE/Ollama, cold SiliconFlow)
- Hard usage cap with auto-downgrade (Opus -> Sonnet -> Haiku -> halt)
- Experience Engine integration with PreToolUse/PostToolUse hooks
- `.muonroi-flow/` artifact system with GSD slash commands
- Session continuity with kill-restart recovery
- Three permission modes: safe, auto-edit, yolo

## Known Issues

| Issue | Severity | Workaround | Status |
|-------|----------|------------|--------|
| Standalone binary does not support OS keychain (keytar) | Low | Use `ANTHROPIC_API_KEY` env var | By design for v1 beta |
| LSP smoke test requires `typescript-language-server` in PATH | Low | Install via `bun add -D typescript-language-server` | Documented |
| Qdrant health check in `doctor` reports warn when Qdrant not running | Info | Expected — Qdrant is optional for local v1 use | By design |
| `install.sh` requires manual repo path update for self-hosted forks | Low | Edit APP/REPO vars in install.sh | Will fix in v1.0 |

## Beta Enrollment

### Prerequisites

- **Bun** >= 1.3.13 (`bun --version`)
- **OS:** Windows 10+, macOS, or Linux
- **API Key:** At least one provider API key (Anthropic recommended)

### Installation

**From npm (recommended):**
```bash
npm install -g muonroi-cli
```

**From source:**
```bash
git clone https://github.com/muonroi/muonroi-cli.git
cd muonroi-cli
bun install
bun run dev
```

**Standalone binary:**
Download from [GitHub Releases](https://github.com/muonroi/muonroi-cli/releases). Set `ANTHROPIC_API_KEY` env var (keychain not available in standalone binary).

### First Run

```bash
muonroi-cli                    # Interactive TUI
muonroi-cli doctor             # Check environment health
muonroi-cli --prompt "hello"   # Headless mode
```

### Reporting Issues

1. Run `muonroi-cli doctor` and copy the output
2. Run `muonroi-cli bug-report > report.json` for anonymized diagnostics
3. Open a GitHub issue using the bug report template
4. **Never share API keys** — `bug-report` auto-redacts, but double-check any additional logs

## Rollout Plan

| Phase | Target | Status |
|-------|--------|--------|
| Phase 0: Fork & Skeleton | Dev environment boots | Complete |
| Phase 1: Brain & Cap Chain | Multi-provider + routing + cap | Complete |
| Phase 2: Continuity & Slash Commands | Flow artifacts + session resume | Complete |
| Phase 3: Polish & Beta | CI matrix + binaries + operator tools | In Progress |
| Phase 4: Cloud & Billing | Multi-tenant + Stripe + dashboard | Planned |

## Support

- **Issues:** [GitHub Issues](https://github.com/muonroi/muonroi-cli/issues)
- **Self-diagnosis:** `muonroi-cli doctor`
- **Bug reports:** `muonroi-cli bug-report`
```
  </action>
  <verify>
    <automated>cd D:/Personal/Core/muonroi-cli && test -f STATUS.md && grep -c "Beta" STATUS.md && grep -c "Known Issues" STATUS.md && grep -c "Rollout Plan" STATUS.md</automated>
  </verify>
  <acceptance_criteria>
    - STATUS.md exists at repo root
    - STATUS.md contains `## Known Issues` section with a table
    - STATUS.md contains `## Beta Enrollment` section with installation instructions
    - STATUS.md contains `## Rollout Plan` section with phase table
    - STATUS.md contains `muonroi-cli doctor` reference
    - STATUS.md contains `muonroi-cli bug-report` reference
    - STATUS.md contains `ANTHROPIC_API_KEY` env var guidance for standalone binary
  </acceptance_criteria>
  <done>STATUS.md at repo root documents known issues, beta enrollment with 3 install methods, reporting instructions, and rollout plan showing all 5 phases.</done>
</task>

</tasks>

<verification>
- `test -f .github/ISSUE_TEMPLATE/bug_report.yml` succeeds
- `test -f .github/ISSUE_TEMPLATE/feature_request.yml` succeeds
- `test -f STATUS.md` succeeds
- `grep "doctor" .github/ISSUE_TEMPLATE/bug_report.yml` returns matches
- `grep "Known Issues" STATUS.md` returns a match
</verification>

<success_criteria>
- Bug report template requires doctor output and references auto-redaction
- Feature request template has structured fields
- STATUS.md covers known issues, beta enrollment (3 install methods), and rollout plan
- All files exist at correct paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-headless-cross-platform-beta/03-05-SUMMARY.md`
</output>
