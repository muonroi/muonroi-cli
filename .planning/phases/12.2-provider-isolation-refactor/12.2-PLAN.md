# Phase 12.2: Provider Isolation Refactor

> Tách provider-specific quirks khỏi shared code bằng ProviderCapabilities strategy pattern. Loại bỏ `if/===` rải rác trong orchestrator, runtime, prompts, forensics.

---

## Motivation

CLI support 7 provider (anthropic, openai, google, deepseek, siliconflow, xai, ollama). Mỗi provider có quirk riêng. Hiện trạng còn **6 dạng leak**:

- `orchestrator.ts:1117, 3191` — `if (runtime.modelInfo?.responsesOnly)` 2 chỗ
- `orchestrator.ts:1122, 1349, 1377, 1488, 3206, 3207, 3229` — `supportsClientTools === false` 7 chỗ
- `orchestrator.ts:1148, 3257` + `compaction.ts:539` — `supportsMaxOutputTokens === false` 3 chỗ
- `orchestrator.ts:1481, 4342` — `=== "siliconflow"` → `stripReasoningForSiliconflow`
- `orchestrator.ts:4244` — `if (turnProvider === "openai")` emit `promptCacheKey`
- `runtime.ts:185, 196, 206, 216` — branching cho responses API + reasoning effort
- `prompts.ts:252` — `providerId !== "anthropic"` system prompt branch
- `cost-forensics.ts:228` — `model.startsWith("deepseek")` cache hit detection
- `product-loop/index.ts:1133` + 3 file khác — duplicate `ALL_PROVIDERS` array

Provider-specific code chui vào shared code → fix chỗ này dễ làm vỡ chỗ kia.

## Success Criteria

1. Zero `=== "anthropic"|"openai"|"google"|"deepseek"|"siliconflow"|"xai"|"ollama"` literal trong `src/orchestrator/`, `src/council/`, `src/product-loop/`, `src/cost-forensics.ts`, `src/prompts.ts`
2. Zero `modelInfo?.supports*` / `modelInfo?.responsesOnly` check ngoài `src/providers/`
3. `src/providers/capabilities.ts` mở rộng từ 1 method lên ~9 method
4. `src/providers/strategies/<provider>.ts` (8 file) — mỗi provider 1 strategy class
5. `src/providers/runtime.ts` rút thành thin dispatcher (~80 dòng từ 303)
6. Mỗi capability mới có test trong `src/providers/__tests__/capabilities-<provider>.test.ts`
7. `bunx tsc --noEmit` zero error
8. `bunx vitest run` toàn bộ unit suite pass
9. `bunx vitest -c vitest.harness.config.ts run tests/harness/cost-leak-{f1,g1,b3,b4}.spec.ts` pass — zero regression cost-leak
10. **Không động `ModelInfo` flags** (data layer giữ nguyên)
11. **Không thay `unsupportedParams`** runtime override trong `ResolvedModelRuntime`

## Constraints / Anti-goals

- Không refactor `orchestrator.ts` body (đó là Phase 12.1 + future)
- Không đổi `catalog.json` hay `ModelInfo` schema
- Không touch OAuth flow trong `cli/keys.ts` (đã có `IOAuthFlow` interface)
- Không touch `chat/factory.ts` (Discord/Slack — khác domain)

---

## Plans (5 group theo priority)

### Plan 12.2-G1: Mở rộng `ProviderCapabilities` + thay capability-flag check

**Goal:** Wrap 12 capability-flag check rải rác thành 3 method trên `ProviderCapabilities`. Risk thấp nhất — flag đã có trên `ModelInfo`, chỉ thay cách đọc.

**Files:**
- MODIFY `src/providers/capabilities.ts` — thêm 3 method:
  - `supportsClientTools(model: ModelInfo): boolean` — default true, override false cho responsesOnly providers
  - `usesResponsesAPI(model: ModelInfo): boolean` — default false, override true cho openai-responses + responsesOnly models
  - `acceptsParam(param: "maxOutputTokens"|"temperature"|"topP", model: ModelInfo): boolean` — default true, override false dựa trên `supportsMaxOutputTokens`
- MODIFY `src/orchestrator/orchestrator.ts` — thay 12 check:
  - L1117, L3191 → `capabilities.usesResponsesAPI(runtime.modelInfo)`
  - L1122, L1349, L1377, L1488, L3206, L3207, L3229 → `capabilities.supportsClientTools(runtime.modelInfo)`
  - L1148, L3257 → `capabilities.acceptsParam("maxOutputTokens", runtime.modelInfo)`
- MODIFY `src/orchestrator/compaction.ts:539` → same `acceptsParam` check
- MODIFY `src/providers/runtime.ts:275` (`shouldDropParam`) — delegate sang `capabilities.acceptsParam`
- NEW `src/providers/__tests__/capabilities-flags.test.ts` — verify mỗi provider trả đúng flag

**Risk:** Thấp. Flag logic giữ nguyên, chỉ wrap.

**Verification:**
- `bunx tsc --noEmit` clean
- `bunx vitest run src/providers/__tests__/`
- `bunx vitest run src/orchestrator/`

---

### Plan 12.2-G2: `sanitizeHistory` hook — isolate siliconflow strip

**Goal:** Loại bỏ `if (provider === "siliconflow")` trong orchestrator. Move logic vào capability.

**Files:**
- MODIFY `src/providers/capabilities.ts` — thêm:
  - `sanitizeHistory(messages: ModelMessage[]): ModelMessage[]` — default identity, override trong SiliconflowCapabilities
- NEW `src/providers/capabilities/siliconflow.ts` — extract SiliconflowCapabilities class (hiện đang inline trong capabilities.ts)
- MOVE `src/providers/siliconflow-history.ts` → wrap thành method của SiliconflowCapabilities
- MODIFY `src/orchestrator/orchestrator.ts:1481, 4342` — gỡ `=== "siliconflow"` branch, thay bằng `messages = capabilities.sanitizeHistory(messages)` unconditional (default no-op cho providers khác)
- NEW `src/providers/__tests__/capabilities-sanitize.test.ts`

**Risk:** Thấp. Hiện đã có test `siliconflow-history` — chỉ relocate call site.

**Verification:** `bunx vitest run` toàn bộ suite + harness `cost-leak-*` smoke.

---

### Plan 12.2-G3: `buildProviderOptions` strategy — gom providerOptions assembly

**Goal:** Gom 3 nguồn provider-specific options vào 1 method:
- `openai.promptCacheKey` (orchestrator:4244)
- `anthropic` cache control (runtime.ts:196)
- `xai`/`openai` reasoning effort (runtime.ts:206, 216)

**Files:**
- MODIFY `src/providers/capabilities.ts` — thêm:
  - `buildProviderOptions(ctx: { sessionId?: string; model: ModelInfo; turnIndex?: number }): Record<string, unknown> | undefined`
- Implement override cho từng provider:
  - `AnthropicCapabilities` — cacheControl logic
  - `OpenAICapabilities` — promptCacheKey (sha256 prefix theo session) + reasoning effort
  - `XAICapabilities` — reasoning effort
- MODIFY `src/providers/runtime.ts` — gỡ L185-217 branching, gọi `capability.buildProviderOptions()` thay vào
- MODIFY `src/orchestrator/orchestrator.ts:4244` — gỡ openai branch, vì `providerOptions` đã có sẵn promptCacheKey từ runtime
- MODIFY `src/orchestrator/orchestrator.ts:2121, 2179, 3244` — spread `runtime.providerOptions` không cần biết shape
- MODIFY `src/council/llm.ts:267, 402, 548` (3 chỗ) — same spread, giữ nguyên (đã đúng pattern)
- NEW `src/providers/__tests__/capabilities-provider-options.test.ts`

**Risk:** Trung bình. Phải verify `tests/harness/cost-leak-f1*.spec.ts` (promptCacheKey stability) + `cost-leak-g1.spec.ts` (OAuth unsupported param drop).

**Verification:** Full harness suite, đặc biệt `cost-leak-{f1,g1}.spec.ts`.

---

### Plan 12.2-G4: Rút `runtime.ts` thành thin dispatcher + tạo `src/providers/strategies/`

**Goal:** `runtime.ts` đang chứa branching logic — sai responsibility. Rút thành dispatcher gọi `strategy.resolve()`.

**Files:**
- NEW `src/providers/strategies/base.strategy.ts` — `ProviderStrategy` interface:
  ```ts
  interface ProviderStrategy {
    readonly id: ProviderId;
    resolve(modelInfo: ModelInfo, opts: ResolveOpts): ResolvedModelRuntime;
    capabilities: ProviderCapabilities;
  }
  ```
- NEW `src/providers/strategies/anthropic.strategy.ts`
- NEW `src/providers/strategies/openai.strategy.ts` — handle responses-API + OAuth headers
- NEW `src/providers/strategies/google.strategy.ts`
- NEW `src/providers/strategies/deepseek.strategy.ts`
- NEW `src/providers/strategies/siliconflow.strategy.ts` — extends deepseek
- NEW `src/providers/strategies/xai.strategy.ts`
- NEW `src/providers/strategies/ollama.strategy.ts`
- NEW `src/providers/strategies/registry.ts` — `PROVIDER_STRATEGIES: Record<ProviderId, ProviderStrategy>`
- MODIFY `src/providers/runtime.ts` — rút từ 303 dòng xuống ~80 dòng:
  ```ts
  export function resolveModelRuntime(factory, modelId): ResolvedModelRuntime {
    const modelInfo = getModelInfo(modelId);
    const strategy = PROVIDER_STRATEGIES[modelInfo?.provider ?? "anthropic"];
    return strategy.resolve(modelInfo, { factory, modelId });
  }
  ```
- KEEP `createProviderFactory` trong runtime.ts (vẫn là factory pattern OK), HOẶC move sang `adapter.ts` (đã có pattern tương tự)
- MODIFY `src/providers/runtime-mock.spec.ts` — verify mock interop với strategy registry
- NEW `src/providers/__tests__/strategies-<provider>.test.ts` × 7

**Risk:** Trung bình. `runtime-mock.spec.ts` cần update — đây là spec quan trọng nhất vì mock-model H1 framework dùng nó.

**Verification:** 
- `bunx vitest run src/providers/`
- `bunx vitest -c vitest.harness.config.ts run tests/harness/` (full harness)

---

### Plan 12.2-G5: Sweep — xoá duplicate arrays + cosmetic capabilities

**Goal:** Single source of truth cho provider list + cosmetic per-provider data.

**Files:**
- MODIFY `src/providers/types.ts` — export `ALL_PROVIDER_IDS: readonly ProviderId[]` + `iterProviders()`
- MODIFY `src/cli/config/screen-models.ts:14` — import `ALL_PROVIDER_IDS`
- MODIFY `src/cli/config/screen-providers.ts:15` — import (giữ logic ollama-special)
- MODIFY `src/product-loop/index.ts:1133` — import
- MODIFY `src/providers/adapter.ts:109-115` — import (gỡ duplicate)
- MODIFY `src/providers/capabilities.ts` — thêm:
  - `consoleSignupURL(): string` — Anthropic console, OpenAI platform, etc.
  - `cacheMetricLayout(): { readField: string; creationSupported: boolean }`
  - `systemPromptStyle(): "anthropic" | "openai" | "generic"`
- MODIFY `src/index.ts:1009, 1017` — `capability.consoleSignupURL()` thay `consoleUrlFor("anthropic")`
- MODIFY `src/orchestrator/prompts.ts:252` — gỡ `providerId !== "anthropic"` branch, dùng `capability.systemPromptStyle()`
- MODIFY `src/cli/cost-forensics.ts:228` — dùng `capability.cacheMetricLayout()` thay `startsWith("deepseek")`
- NEW `src/providers/__tests__/capabilities-cosmetic.test.ts`

**Risk:** Thấp. Mostly mechanical replace.

**Verification:** `bunx tsc --noEmit` + grep verification:
```bash
# Acceptance grep — phải trả 0 kết quả:
grep -rnE '=== "(anthropic|openai|google|deepseek|siliconflow|xai|ollama)"' src/orchestrator src/council src/product-loop src/cli/cost-forensics.ts
```

---

## Execution Order

```
G1 (safest) → G2 → G3 → G4 (biggest) → G5 (sweep)
```

Mỗi group:
1. Commit atomic
2. `bunx tsc --noEmit`
3. Relevant test suite pass
4. Harness smoke (G3, G4 phải full)
5. Move to next group

## Dependencies

- Phase 12.1 (orchestrator-refactor) — không hard depend, nhưng cùng touch `orchestrator.ts` nên ưu tiên xong 12.1 trước. Hiện 12.1 đã shipped.

## Risk Assessment

| Risk | Group | Level | Mitigation |
|---|---|---|---|
| Hidden capability check bị bỏ sót | G1 | Low | Grep audit step ở G5 sẽ catch |
| siliconflow sanitize call site sai thứ tự | G2 | Low | Test `siliconflow-history` đã verify behavior |
| promptCacheKey stability breaks F1 spec | G3 | Medium | Run `cost-leak-f1.spec.ts` ngay sau commit |
| runtime-mock.spec.ts break mock-model | G4 | Medium | Run `runtime-mock.spec.ts` first |
| Duplicate array drift sau merge | G5 | Low | TypeScript readonly tuple + import-only |

## Out of scope (defer)

- Refactor `runTaskRequest` body (Phase 12.x tiếp theo)
- Đổi `ModelInfo` schema (catalog versioning rủi ro cao)
- Per-provider rate limiting / circuit breaker (Phase 21 EE observability)
- OAuth IOAuthFlow refactor — đã đúng pattern
