---
phase: 13-product-ideal-loop
plan: 06
type: execute
wave: 5
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05"]
files_modified:
  - src/ui/slash/ideal.ts
  - src/orchestrator/orchestrator.ts
  - src/product-loop/sprint-runner.ts
  - src/product-loop/feedback-routing.ts
  - src/product-loop/index.ts
  - src/ui/cards/product-status-card.tsx
  - src/product-loop/__tests__/sprint-runner.test.ts
  - src/product-loop/__tests__/feedback-routing.test.ts
  - src/product-loop/__tests__/integration.test.ts
  - src/ui/slash/__tests__/ideal.test.ts
autonomous: true
requirements: [PIL-13-6]
must_haves:
  truths:
    - "/ideal \"<idea>\" starts a new product run; status/resume/abort/ship subcommands work"
    - "Orchestrator exposes runProductLoopV1 mirroring runCouncilV2"
    - "Sprint runner: plan (council) → implement → verify → judge → done-gate, with feedback routing on continue"
    - "product_status_card renders sprint progress + cost + criteria checklist"
    - "MUONROI_DEV=1 enables --no-customer-debate; flag NOT shown in --help"
    - "Resume reconstructs from 6 artifact files and re-enters correct stage"
    - "End-to-end integration test runs a mini-loop with mocked LLM and reaches ready_to_sprint or done"
  artifacts:
    - path: src/ui/slash/ideal.ts
      provides: handleIdealSlash with subcommands (start, status, resume, abort, ship) + flag parsing via commander
    - path: src/orchestrator/orchestrator.ts
      provides: runProductLoopV1(idea, flags, options?) async generator (mirror of runCouncilV2 at line 2037)
    - path: src/product-loop/sprint-runner.ts
      provides: runSprint(sprintN, ctx) — plan/implement/verify/judge cycle, returns IterationState
    - path: src/product-loop/feedback-routing.ts
      provides: buildContinueFeedback(verdict, lastVerify, criteria) — maps failed condition to next-sprint focus
    - path: src/ui/cards/product-status-card.tsx
      provides: TUI renderer for the new product_status_card chunk type
  key_links:
    - from: src/ui/slash/ideal.ts
      to: src/orchestrator/orchestrator.ts runProductLoopV1
      via: returns sentinel string "__PRODUCT_LOOP__\n<json-args>" parsed by app dispatcher (mirror council pattern)
    - from: runProductLoopV1
      to: src/product-loop/index.ts runProductLoop
      via: dynamic import + AsyncGenerator yield-pump (mirrors runCouncilV2 lines 2068-2074)
    - from: sprint-runner
      to: runCouncil (planner) + runVerifyOrchestration + done-gate + circuit-breakers + cost-scoper + phase-tracker-bridge
      via: orchestration of all 13-02..13-05 components
---

<objective>
Final wiring plan: registers the `/ideal` slash command, wires `runProductLoopV1` into the orchestrator,
implements the sprint inner loop (plan→implement→verify→judge→done-gate), feedback routing for failed
done conditions, the `product_status_card` TUI renderer, and an end-to-end integration test that runs
a mini-loop with a mocked LLM.

Purpose: Ship the user-facing surface and complete the FSM. Delivers ROADMAP success criteria #1, #7,
#8, #9, and #10 (everything not already delivered by 13-01..13-05).

CRITICAL PATH CORRECTION: The slash command lives at `src/ui/slash/ideal.ts` (NEW), not
`src/cli/commands.ts` (does not exist — RESEARCH §1 confirmed). Pattern mirrors `src/ui/slash/council.ts`.

Output:
- `src/ui/slash/ideal.ts` (NEW, ~120 LoC) — slash registration + commander flag parsing + subcommands
- `src/orchestrator/orchestrator.ts` edit (~+55 LoC) — `runProductLoopV1` method
- `src/product-loop/sprint-runner.ts` (NEW, ~250 LoC) — sprint inner loop
- `src/product-loop/feedback-routing.ts` (NEW, ~100 LoC) — failure → next-sprint focus mapping
- `src/product-loop/index.ts` edit (~+80 LoC) — extend with sprint loop, resume support, abort/ship subcommands
- `src/ui/cards/product-status-card.tsx` (NEW, ~80 LoC) — Ink renderer (mirror council_status renderer)
- 4 test files (~400 LoC) including integration
</objective>

<execution_context>
TDD where feasible (sprint-runner, feedback-routing, ideal.ts arg parsing). Integration test uses
mocked CouncilLLM and a fake bash to avoid network/IO. TUI renderer is React/Ink — snapshot-test
acceptable.
</execution_context>

<context>
@.planning/phases/13-product-ideal-loop/13-CONTEXT.md
@.planning/phases/13-product-ideal-loop/13-RESEARCH.md
@.planning/phases/13-product-ideal-loop/13-01-PLAN.md
@.planning/phases/13-product-ideal-loop/13-02-PLAN.md
@.planning/phases/13-product-ideal-loop/13-03-PLAN.md
@.planning/phases/13-product-ideal-loop/13-04-PLAN.md
@.planning/phases/13-product-ideal-loop/13-05-PLAN.md
@src/ui/slash/council.ts
@src/ui/slash/registry.ts
@src/orchestrator/orchestrator.ts (lines 2037-2082)
@src/council/index.ts

<interfaces>
Slash registration pattern (mirror of `src/ui/slash/council.ts`):

```ts
import type { SlashHandler } from "./registry.js";
import { registerSlash } from "./registry.js";

export const handleIdealSlash: SlashHandler = async (args, ctx) => {
  // 1. Detect subcommand: status | resume | abort | ship | (default = start)
  // 2. Parse flags via commander (already in package.json deps): --max-cost, --max-sprints, --done-threshold, --stack
  // 3. Return sentinel string for app.tsx to parse
  return `__PRODUCT_LOOP__\n${JSON.stringify({ subcommand, idea, flags })}`;
};
registerSlash("ideal", handleIdealSlash);
```

Orchestrator wiring (mirror runCouncilV2 at lines 2037-2082, locked snippet from CONTEXT.md):

```ts
async *runProductLoopV1(idea: string, flags: ProductLoopFlags, options?: { observer?, userModelMessage? }): AsyncGenerator<StreamChunk, void, unknown> {
  const { runProductLoop } = await import("../product-loop/index.js");
  const { createCouncilLLM } = await import("../council/llm.js");
  const productStats = { calls: 0, startMs: Date.now(), phases: [] as Array<{ name: string; durationMs: number }> };
  const llm = createCouncilLLM(this.bash, this.mode, this.session?.id, productStats);
  const processMessageFn = (m: string) => this.processMessage(m, options?.observer);
  const gen = runProductLoop({
    idea, flags,
    cwd: this.bash.getCwd(),
    flowDir: path.join(this.bash.getCwd(), ".muonroi-flow"),
    sessionId: this.session?.id,
    llm,
    processMessageFn,
    respondToQuestion: this._createQuestionResponder(),
    respondToPreflight: this._createPreflightResponder(),
  });
  for await (const chunk of gen) yield chunk;
}
```

Sprint loop body (per CONTEXT.md FSM):
sprint-N: plan → implement → verify → judge → (retrospective | halted | sprint-N+1: plan) → shipped

Continue feedback routing (per CONTEXT.md):
| Failed Cond | Next sprint focus |
|---|---|
| #1 | "fix verify failures" + paste lastVerify.detail |
| #2 | "evidence missing for criteria X, Y" — Tester role assigned |
| #3 | "score N%, gap = unmet criteria [X,Y,Z]" — PO prioritize |
| #4 | "Customer disagrees: <reason>" — Architect/Implementer iterate |
| #5 | "user feedback: <text>" — full re-plan |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: feedback-routing.ts (failed condition → next sprint focus)</name>
  <files>src/product-loop/feedback-routing.ts, src/product-loop/__tests__/feedback-routing.test.ts</files>
  <behavior>
    - buildContinueFeedback(verdict: DoneVerdict, lastVerify: ToolResult | null, criteria: Criterion[]): { focus: string, assignedRole?: RoleSlot }
    - For each of 5 failed conditions, returns the locked focus text from CONTEXT.md table
    - Cond #2: assignedRole="Tester"
    - Cond #3: assignedRole="PO"
    - Cond #4: assignedRole="Architect" (or "Implementer" depending on dissent reason)
    - Cond #5: free-form user feedback piped through verdict.reason
  </behavior>
  <action>
    1. Test all 5 failure paths with a fixture verdict + canned lastVerify/criteria.
    2. Implement as a switch over verdict.failedCondition.
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/feedback-routing.test.ts</automated>
  </verify>
  <done>
    - All 5 conditions covered
    - Output is deterministic for fixed input
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sprint-runner.ts (plan→implement→verify→judge inner loop)</name>
  <files>src/product-loop/sprint-runner.ts, src/product-loop/__tests__/sprint-runner.test.ts</files>
  <behavior>
    runSprint({ sprintN, ctx, productSpec, runId, flowDir, llm, ... }): AsyncGenerator<StreamChunk, IterationState, unknown>

    Stages emitted as StreamChunk content events:
    1. plan: invoke runCouncil with skipClarification=true and the productSpec + carry-over context
    2. implement: invoke processMessageFn with the plan output (existing code-execution path)
    3. verify: invoke runVerifyOrchestration; parse result with parseVerifyResult; populate recipe.coverage from output
    4. judge: invoke EE judge.ts on tool calls during implement (already wired via posttool)
    5. circuit-breaker check: CB-1, CB-2, CB-3 — halt with reason if any fires
    6. done-gate: evaluateDoneGate; if pass → shipped; else buildContinueFeedback for next sprint
    7. cost: reserveForProduct + commitToProduct around each LLM call (use 13-05's cost-scoper)
    8. boundary: postSprintBoundary at end of each sprint with kind=pass|fail|aborted

    Side effects:
    - appendIteration writes the sprint's IterationState to iterations.md (machine-parseable for CB-2)
    - update gray-areas.md criteria statuses with evidence after verify
    - update state.md Resume Digest with currentSprint, currentStage, lastVerify
    - update per-role memory (role-memory.ts) at end of sprint
  </behavior>
  <action>
    1. Tests first (mocked everything):
       - Happy sprint: all stages run, returns IterationState with verify=PASS, score increases
       - CB-3 trip on sprint 1 (recipe null) → halt before plan stage even completes (well, after verify probe — CB-3 fires when recipe profile is detected as missing). Decide ordering: detect recipe BEFORE first sprint to support CB-3 deterministically.
       - CB-1 trip on sprint 4: halt with "cost projection exceeds headroom"
       - CB-2 trip on sprint 5 with non-positive deltas at sprints 4,5: halt with "oscillation"
       - Done-gate Cond #1 fail: returns IterationState.done=false, with feedback="fix verify failures"
    2. Use dependency injection for every external module (council, verify, ee/judge, cost-scoper, done-gate, circuit-breakers) so the test can stub them all.
    3. Order matters: cost reserve → LLM call → cost commit. Use try/finally for release on error.
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/sprint-runner.test.ts</automated>
  </verify>
  <done>
    - 5+ test cases green covering happy path + each circuit breaker + Cond #1 fail
    - Iteration state appended to iterations.md correctly (verify with readIterations)
    - Cost reservation released on error path (no leaked reservations)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ideal.ts slash command + commander flag parsing</name>
  <files>src/ui/slash/ideal.ts, src/ui/slash/__tests__/ideal.test.ts</files>
  <behavior>
    Subcommands (first arg or default):
    - `/ideal "<idea>"` (default = start) — flags: --max-cost (1-1000, default 50), --max-sprints (1-20, default 8), --done-threshold (0.7-1.0, default 0.9), --stack <free text>
    - `/ideal status` (no runId = list active)
    - `/ideal status <runId>`
    - `/ideal resume <runId>`
    - `/ideal abort <runId>`
    - `/ideal ship <runId>`

    Returns sentinel string `__PRODUCT_LOOP__\n${JSON.stringify({ subcommand, idea?, runId?, flags })}` — app.tsx dispatch parses and calls orchestrator method (mirror of how `__COUNCIL__` is handled).

    Flag clamping:
    - --max-cost out of range → returns help text + error
    - --max-sprints out of range → returns help text + error
    - --done-threshold out of [0.7, 1.0] → clamp + emit warning in help text

    MUONROI_DEV=1 env var enables `--no-customer-debate`. NOT documented in `--help`.
  </behavior>
  <action>
    1. Tests first:
       - `/ideal "build a CLI for X"` → subcommand=start, idea set
       - `/ideal status` → subcommand=status, no runId
       - `/ideal resume abc123` → subcommand=resume, runId=abc123
       - `/ideal --max-cost 999 "idea"` → flags.maxCost=999
       - `/ideal --max-cost 9999 "idea"` → returns help with range error
       - `/ideal` (no args) → returns help text mentioning subcommands + flags
    2. Use commander.Command with `.exitOverride()` to prevent process.exit on parse error (commander already in deps per RESEARCH §4).
    3. Reference dev hatch in code comments only, NOT in commander's help text:
       ```ts
       // Internal dev escape hatch: MUONROI_DEV=1 enables --no-customer-debate.
       // Intentionally not registered with commander to keep it out of --help.
       const noCustomerDebate = process.env.MUONROI_DEV === "1";
       ```
    4. Self-register: `registerSlash("ideal", handleIdealSlash);` at file end.
  </action>
  <verify>
    <automated>npx vitest run src/ui/slash/__tests__/ideal.test.ts</automated>
  </verify>
  <done>
    - All 6 test cases green
    - `--no-customer-debate` does NOT appear in --help output
    - Flag clamping enforces locked ranges
  </done>
</task>

<task type="auto">
  <name>Task 4: orchestrator.runProductLoopV1 + product_status_card TUI renderer</name>
  <files>src/orchestrator/orchestrator.ts, src/ui/cards/product-status-card.tsx</files>
  <behavior>
    Orchestrator method `runProductLoopV1(idea, flags, options?)` exact mirror of runCouncilV2 at line 2037.

    TUI renderer for `product_status_card` chunk type:
    - Renders sprintN/totalSprints, cost spent/cap, criteria met/partial/unmet counts, currentStage label
    - Mirror visual style of existing council_status renderer (locate via grep `council_status` in src/ui)
    - Subscribes to chunks of type "product_status_card" from the orchestrator stream
  </behavior>
  <action>
    1. Add the runProductLoopV1 method right below runCouncilV2 (target line ~2083) using the locked snippet from CONTEXT.md "Orchestrator wiring":
       ```ts
       async *runProductLoopV1(idea, flags, options?) {
         const { runProductLoop } = await import("../product-loop/index.js");
         // … (see <interfaces> block above for full snippet)
         for await (const chunk of gen) yield chunk;
       }
       ```
    2. Wire app.tsx (or wherever __COUNCIL__ sentinel is dispatched) to handle `__PRODUCT_LOOP__\n<json>` similarly. Search: `grep -n "__COUNCIL__" src/ui src/orchestrator src/app*` to find dispatcher.
    3. Find existing council_status renderer (likely `src/ui/cards/council-status-card.tsx` or in `src/ui/App.tsx`). Mirror its shape for product-status-card.tsx.
    4. Test: existing council e2e test must still pass (no regression to runCouncilV2).
  </action>
  <verify>
    <automated>npx vitest run src/orchestrator src/ui</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` passes
    - runProductLoopV1 exists and compiles
    - product_status_card renderer registered (verify by spawning a chunk in a unit test)
    - No council regression
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: index.ts orchestration + resume/abort/ship + integration test</name>
  <files>src/product-loop/index.ts, src/product-loop/__tests__/integration.test.ts</files>
  <behavior>
    runProductLoop entry orchestrates the full flow:
    1. Parse subcommand from opts.flags
    2. start: createRun → resolveRoles (refuse if collision) → runLoopDriver (gather/research/scoping) → sprint loop until done or halt → final user approval
    3. status: list runs from .muonroi-flow/runs/, parse each manifest.md, format table
    4. resume: loadRun → parse state.md for currentStage/currentSprint → re-enter at stage. For in-flight sprint, mark old as crashed (markIterationCrashed) and start fresh sprint with retryOf=N. Fire phase-outcome=resumed.
    5. abort: write manifest.md aborted=true, fire phase-outcome=aborted, set Customer/PO release. Final state.
    6. ship: skip Cond #1-#4 if all already pass; force Cond #5 (user approval) directly. If not yet at done state, refuse with "not ready to ship".

    Integration test covers a 1-sprint mini-loop with mocked LLM that:
    - Resolves all 6 gather dimensions in round 1
    - Skips research (mock leader returns "no debate needed")
    - Scoping returns minimal ProductSpec; preflight #1 approved
    - Sprint 1 runs: plan → implement (writes a stub file) → verify (mock returns PASS) → done-gate passes (or fails Cond #5 in negative test)
  </behavior>
  <action>
    1. Tests:
       - Integration happy path: 1 sprint to done; all 6 artifact files contain expected sections
       - Resume: kill mid-sprint (simulate by writing iterations.md without `Verify:` line) → resume detects crashed sprint, marks it, restarts as sprint N with retryOf
       - Abort: writes manifest.md aborted=true, fires phase-outcome=aborted (mocked)
       - Ship: pre-passing run reaches Cond #5 directly; non-passing run refuses with clear message
    2. Reuse all 13-01..13-05 modules — this is a wiring task, minimize new logic.
    3. Use a mocked CouncilLLM that returns deterministic canned responses by prompt-pattern. Avoid any real network call.
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/integration.test.ts</automated>
  </verify>
  <done>
    - Integration test green: 1-sprint loop reaches done state with all 6 files written
    - Resume test green: detects crashed sprint, restarts cleanly
    - Abort test green: manifest aborted=true, phase-outcome=aborted fired
    - Ship test green: pre-passing run skips Cond #1-#4
  </done>
</task>

</tasks>

<verification>
Full phase 13 regression:
```
npx tsc --noEmit
npx vitest run
```

Manual smoke (after `bun run build` if build is bun-based, else `npm run build`):
1. Run the CLI in a clean tmp dir.
2. `/ideal "todo list app"` — expect gather card to appear, answer 6 dimensions.
3. After scoping preflight, approve. Sprint 1 runs.
4. After sprint 1 finishes (verify mocked or real depending on cwd), inspect `.muonroi-flow/runs/<id>/`:
   - all 6 files present (roadmap, state, delegations, gray-areas, iterations, manifest)
   - manifest.md contains idea, capUsd, createdAt
   - iterations.md contains Sprint 1 block with Score/Cost/Verify lines
5. `/ideal status` — lists the run.
6. `/ideal abort <id>` — manifest.md updated `aborted: true`.

Coverage table check (success criteria → plans):
| ROADMAP Success Criterion | Delivered by Plan(s) |
|---|---|
| 1. /ideal creates 6 artifact files | 13-01 (run-manager extension) + 13-03 (createRun call) + 13-06 (slash invokes runProductLoop) |
| 2. Gather refuses < 5/6 in 6 rounds | 13-03 (loop-driver gather stage) |
| 3. Done-gate 5 conditions cost-ascending short-circuit | 13-04 (done-gate.ts) |
| 4. PO ↔ Customer distinct models | 13-02 (role-registry resolution) |
| 5. 3 circuit breakers fire deterministically | 13-04 (circuit-breakers.ts) |
| 6. Per-product ledger writes alongside monthly | 13-05 (product-ledger + commitToProduct) |
| 7. Resume reconstructs from 6 files | 13-06 (sprint-runner resume + index.ts subcommand) |
| 8. EE phase-tracker + PIL Layer 5 integration | 13-05 (phase-tracker-bridge) + 13-06 (driver calls bridge) — PIL Layer 5 already reads "Resume Digest" from state.md per RESEARCH §1 |
| 9. Council/verify/ee zero-edits; runProductLoopV1 mirrors runCouncilV2 | 13-06 (orchestrator.ts edit). Council: parameterize MAX_CLARIFICATION_ROUNDS in 13-03 is a 1-line additive edit, default behavior preserved. Verify: VerifyRecipe.coverage extension in 13-04 is additive optional field. |
| 10. MUONROI_DEV=1 enables --no-customer-debate, not in --help | 13-04 (done-gate honors env var) + 13-06 (slash does not register flag in commander) |
</verification>

<success_criteria>
- ROADMAP success criterion #1 — DELIVERED (createRun + 6 files now invoked from /ideal)
- ROADMAP success criterion #7 — DELIVERED (resume reconstructs from 6 files)
- ROADMAP success criterion #8 — DELIVERED (phase-tracker fires on sprint boundary; PIL Layer 5 already reads Resume Digest)
- ROADMAP success criterion #9 — DELIVERED (runProductLoopV1 mirrors runCouncilV2; council/verify/ee remain caller-only with two additive optional edits documented)
- ROADMAP success criterion #10 — DELIVERED (MUONROI_DEV=1 honored, not in --help)
</success_criteria>

<output>
13-06-SUMMARY.md with the full 10-criteria coverage table referenced above + a manual-smoke checklist.
</output>
