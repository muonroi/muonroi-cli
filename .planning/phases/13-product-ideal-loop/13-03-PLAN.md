---
phase: 13-product-ideal-loop
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - src/council/clarifier.ts
  - src/product-loop/loop-driver.ts
  - src/product-loop/seed-questions.ts
  - src/product-loop/index.ts
  - src/product-loop/__tests__/loop-driver.test.ts
  - src/product-loop/__tests__/seed-questions.test.ts
  - src/council/__tests__/clarifier-max-rounds.test.ts
autonomous: true
requirements: [PIL-13-3]
must_haves:
  truths:
    - "Loop FSM transitions idle → gather → research → scoping → approved without skipping states"
    - "Gather refuses to advance when fewer than 5/6 dimensions are resolved within 6 rounds"
    - "MAX_CLARIFICATION_ROUNDS is parameterized; product-loop passes 6, council default behavior unchanged"
    - "runProductLoop returns AsyncGenerator<StreamChunk, void, unknown> mirroring runCouncilV2"
  artifacts:
    - path: src/product-loop/loop-driver.ts
      provides: runLoopDriver(initialState) FSM driver yielding StreamChunks
    - path: src/product-loop/seed-questions.ts
      provides: 6 GrayAreaQuestion seed dimensions (persona, core-features, non-functional, tech-constraints, success-metric, cost-tolerance)
    - path: src/product-loop/index.ts
      provides: runProductLoop(opts) entry point — wraps driver, owns runId creation + 6-file init
    - path: src/council/clarifier.ts
      provides: parameterized maxRounds option (default 3, override at call site)
  key_links:
    - from: src/product-loop/index.ts
      to: src/flow/run-manager.ts createRun
      via: creates the run dir + 6 files before driver starts
    - from: src/product-loop/loop-driver.ts gather stage
      to: src/council/clarifier.ts runClarification with maxRounds=6, seedQuestions=SEED_DIMENSIONS
      via: confidence metric = unresolvedDimensions.length, NOT PIL classifier confidence
    - from: src/product-loop/loop-driver.ts research stage
      to: src/council/debate.ts runDebate
      via: 4 leader-proposed stances (Researcher/Cost-Controller/Skeptic/Architect)
    - from: src/product-loop/loop-driver.ts scoping stage
      to: src/council/preflight.ts (council_preflight card)
      via: ProductSpec JSON written to roadmap.md → user approve/reject/edit
---

<objective>
Build the outer FSM driver and the three pre-sprint stages: gather, research, scoping. Parameterize the
council clarifier to accept maxRounds. Sprint loop body is OUT OF SCOPE here — driver yields a "ready to
sprint" terminal chunk; sprint orchestration lives in 13-04 (done-gate calls) + 13-06 (orchestrator wiring).

Purpose: Deliver ROADMAP success criteria #2 (gather refuses to advance until ≥5/6 dimensions resolved
within 6 rounds) and lay the FSM scaffolding 13-04 will plug into.

Output:
- `src/product-loop/loop-driver.ts` (~280 LoC) — hand-rolled switch FSM
- `src/product-loop/seed-questions.ts` (~80 LoC) — 6 hardcoded GrayAreaQuestion objects
- `src/product-loop/index.ts` (~120 LoC) — runProductLoop entry, runId + flowDir wiring
- `src/council/clarifier.ts` edit (~+5 LoC) — parameterize MAX_CLARIFICATION_ROUNDS
- 3 test files
</objective>

<execution_context>
TDD applies. The FSM has 4 pre-sprint states (idle, gather, research, scoping) — write transition tests
before the driver is implemented. Use a mock LLM that returns canned ClarifiedSpec to avoid network in tests.
</execution_context>

<context>
@.planning/phases/13-product-ideal-loop/13-CONTEXT.md
@.planning/phases/13-product-ideal-loop/13-RESEARCH.md
@.planning/phases/13-product-ideal-loop/13-01-PLAN.md
@.planning/phases/13-product-ideal-loop/13-02-PLAN.md
@src/council/clarifier.ts
@src/council/debate.ts
@src/council/preflight.ts
@src/council/index.ts
@src/orchestrator/orchestrator.ts (lines 2037-2082, runCouncilV2 template)

<interfaces>
Existing surfaces:

```ts
// src/council/clarifier.ts:88
export async function* runClarification(
  topic: string,
  leaderModelId: string,
  conversationContext: string,
  respondToQuestion: QuestionResponder,
  llm: CouncilLLM,
  signal?: AbortSignal,
  seedQuestions?: GrayAreaQuestion[],
): AsyncGenerator<StreamChunk, ClarifiedSpec, unknown>;

// src/council/clarifier.ts:86 — TO PARAMETERIZE
const MAX_CLARIFICATION_ROUNDS = 3;  // → make optional 8th arg with default 3

// 6 seed dimensions (from CONTEXT.md "Gather stage"):
// persona | core-features | non-functional | tech-constraints | success-metric | cost-tolerance
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize MAX_CLARIFICATION_ROUNDS in clarifier.ts</name>
  <files>src/council/clarifier.ts, src/council/__tests__/clarifier-max-rounds.test.ts</files>
  <behavior>
    - runClarification gains optional `maxRounds?: number` 8th argument
    - Default = 3 (preserves existing council behavior — verify with existing clarifier tests)
    - When passed 6, the loop iterates up to 6 rounds
    - When passed 0 or negative, falls back to default 3 (defensive)
  </behavior>
  <action>
    1. Test first: spy on respondToQuestion, call runClarification with maxRounds=6, count phaseStart events for clarification rounds → expect ≤ 6.
    2. Edit clarifier.ts:
       - Remove the file-level `const MAX_CLARIFICATION_ROUNDS = 3;`
       - Add `maxRounds?: number` as the 8th parameter
       - Inside body: `const max = (typeof maxRounds === "number" && maxRounds > 0) ? maxRounds : 3;`
       - Replace the existing `for (let round = 0; round < MAX_CLARIFICATION_ROUNDS; round++)` with `for (let round = 0; round < max; round++)`
    3. Run existing council clarifier tests to confirm zero regression.
  </action>
  <verify>
    <automated>npx vitest run src/council/__tests__/clarifier-options.test.ts src/council/__tests__/clarifier-max-rounds.test.ts</automated>
  </verify>
  <done>
    - New test green
    - Existing clarifier-options.test.ts still green (default behavior unchanged)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed-questions.ts with 6 hardcoded GrayAreaQuestion objects</name>
  <files>src/product-loop/seed-questions.ts, src/product-loop/__tests__/seed-questions.test.ts</files>
  <behavior>
    - Exports `SEED_DIMENSIONS: GrayAreaQuestion[]` of length exactly 6
    - Each dimension has stable `id` matching the locked names: "persona", "core-features", "non-functional", "tech-constraints", "success-metric", "cost-tolerance"
    - Each question.text is a complete English sentence ending with `?`
    - `isRequired: true` on all 6
  </behavior>
  <action>
    1. Test first: assert length, ids, all isRequired true.
    2. Author the 6 questions with concrete prompts per CONTEXT.md "Gather stage":
       - persona: "Who are the primary users for this product?"
       - core-features: "What are the top 3 must-have features?"
       - non-functional: "What are the performance, privacy, offline, or scale targets?"
       - tech-constraints: "What language, framework, or existing repo constraints apply?"
       - success-metric: "How will 'done' be measured (e.g., metric, threshold)?"
       - cost-tolerance: "Is there a hard cost cap or a soft target?"
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/seed-questions.test.ts</automated>
  </verify>
  <done>
    - All 6 ids present and stable
    - Text fields are non-empty, end with `?`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: loop-driver.ts FSM with idle/gather/research/scoping stages</name>
  <files>src/product-loop/loop-driver.ts, src/product-loop/index.ts, src/product-loop/__tests__/loop-driver.test.ts</files>
  <behavior>
    FSM transitions:
    - idle → gather: emits phase-start chunk, calls runClarification(maxRounds=6, seedQuestions=SEED_DIMENSIONS)
    - gather: count unresolvedDimensions = SEED_DIMENSIONS.length - resolvedCount
    - gather → research: when unresolvedDimensions ≤ 1 (≥5/6 = 83.3%, threshold honors locked ≥85% via ≥5/6 integer)
    - gather → halted: at round 6 with unresolved > 1, emits a council_question card requesting manual answers, refuses to advance (yields error chunk + resume hint)
    - research → scoping: after debate completes (4 stances Researcher/Cost-Controller/Skeptic/Architect)
    - scoping → approved: leader synthesis writes ProductSpec to roadmap.md, emits council_preflight, awaits user approval
    - scoping → halted: user rejects spec
    - approved: emits a "ready_to_sprint" terminal chunk (sprint loop hooks in 13-04/13-06)

    Side effects per stage:
    - On entry to gather: write `state.md` Resume Digest with stage info
    - On exit from gather: write resolved dimensions to `gray-areas.md` as criteria seeds
    - On exit from research: append research summary to `delegations.md`
    - On exit from scoping: write ProductSpec JSON to `roadmap.md` (extend existing roadmap section)
    - On any transition: write `manifest.md` createdAt + idea (idempotent on first call)
  </behavior>
  <action>
    1. Tests first (use mock CouncilLLM + mock respondToQuestion that returns canned answers):
       - happy path: gather (5/6 resolved round 3) → research → scoping → approved → emits ready_to_sprint
       - gather refuse: 6 rounds completed with only 3/6 resolved → driver emits council_question manual-answers card + halted state
       - scoping reject: user clicks "reject" on preflight → halted with reason "user_rejected_spec"
    2. Implement as a hand-rolled switch FSM (per RESEARCH §4 — no FSM lib in deps):
       ```ts
       async function* runLoopDriver(ctx: DriverContext): AsyncGenerator<StreamChunk, DriverResult, unknown> {
         let state: Stage = "idle";
         while (true) {
           switch (state) { case "idle": ...; case "gather": ...; ... }
         }
       }
       ```
    3. `runProductLoop(opts)` in index.ts:
       - Accepts `{ idea, flags, llm, processMessageFn, respondToQuestion, respondToPreflight }`
       - Calls `createRun(flowDir)` to mint runId + 6 files
       - Writes initial manifest via `writeManifest`
       - Constructs DriverContext + delegates to `runLoopDriver`
       - Yields all chunks; returns final DriverResult
    4. Confidence metric: `unresolvedDimensions = SEED_DIMENSIONS.filter(d => clarifiedSpec.resolved[d.id] !== "answered").length` — NOT PIL classifier confidence (per CONTEXT.md).
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/loop-driver.test.ts</automated>
  </verify>
  <done>
    - Happy path: all 4 stage transitions emit expected chunks in order
    - Gather refuse: at round 6 with insufficient resolution, driver emits halted result without proceeding to research
    - Scoping reject: driver emits halted with reason "user_rejected_spec"
    - All 6 run files modified appropriately by end of approved state
  </done>
</task>

</tasks>

<verification>
```
npx tsc --noEmit
npx vitest run src/council/__tests__/clarifier-options.test.ts \
              src/council/__tests__/clarifier-max-rounds.test.ts \
              src/product-loop/__tests__/seed-questions.test.ts \
              src/product-loop/__tests__/loop-driver.test.ts
```

Manual smoke (deferred to 13-06 which wires real orchestrator).
</verification>

<success_criteria>
- ROADMAP success criterion #2 — DELIVERED (gather refuses to advance with <5/6 in 6 rounds)
- ROADMAP success criterion #1 — PARTIALLY (run dir + 6 files created on entry to runProductLoop)
- Council remains zero-edits-to-behavior (parameterization only, default unchanged)
</success_criteria>

<output>
13-03-SUMMARY.md listing FSM state machine table + clarifier signature change.
</output>
