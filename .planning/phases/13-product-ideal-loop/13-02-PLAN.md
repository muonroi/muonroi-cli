---
phase: 13-product-ideal-loop
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/product-loop/role-registry.ts
  - src/product-loop/role-memory.ts
  - src/product-loop/__tests__/role-registry.test.ts
  - src/product-loop/__tests__/role-memory.test.ts
autonomous: true
requirements: [PIL-13-2]
must_haves:
  truths:
    - "PO and Customer slots resolve to distinct models (cross-provider preferred)"
    - "Same-model assignment for PO == Customer is hard refused at run start"
    - "Per-role memory respects 2KB hard cap with oldest-first truncation"
    - "If only one provider with ≤5 models is configured, run start refuses with clear message"
  artifacts:
    - path: src/product-loop/role-registry.ts
      provides: resolveRoles(opts) -> { roles: Record<RoleSlot, ModelAssignment>, sameProvider: boolean } | RoleResolutionError
    - path: src/product-loop/role-memory.ts
      provides: appendRoleMemory(flowDir, runId, slot, content), readRoleMemory(flowDir, runId, slot) with 2KB enforcement
  key_links:
    - from: src/product-loop/role-registry.ts
      to: src/usage/router.ts (or whatever EE-aware routeModel exists) and src/config/providers.ts
      via: cold-start tier preference + EE override
    - from: role-memory.ts
      to: runs/<id>/memory/<slot>.md via flow/artifact-io.ts
      via: atomic append + truncation
---

<objective>
Implement the 6-role registry with cross-provider/cross-tier resolution and the per-role memory store.

Purpose: Provide the deterministic anti-echo invariant that done-gate Cond #4 (PO ↔ Customer cross-model
debate) and L3 anti-hallucination depend on. Resolution must run BEFORE any sprint starts and refuse the
run if the constraint cannot be satisfied (R2 mitigation).

Output:
- `src/product-loop/role-registry.ts` (~220 LoC)
- `src/product-loop/role-memory.ts` (~120 LoC)
- 2 test files (~250 LoC)
</objective>

<execution_context>
TDD applies. Resolution algorithm has a deterministic specification — write the matrix tests first
(cross-provider, same-provider/different-tier, same-provider-same-tier-different-model, single-provider-with-≤5-models),
then implement.
</execution_context>

<context>
@.planning/phases/13-product-ideal-loop/13-CONTEXT.md
@.planning/phases/13-product-ideal-loop/13-RESEARCH.md
@.planning/phases/13-product-ideal-loop/13-01-PLAN.md
@src/product-loop/types.ts
@src/flow/artifact-io.ts

<interfaces>
Tier preference (locked, from CONTEXT.md "Role registry"):
- PO, Architect, Reviewer, Customer: ["premium", "balanced"]
- Implementer:                       ["balanced", "fast", "premium"]
- Tester:                            ["balanced", "premium"]

Resolution passes (locked):
- Pass 1: cross-provider for PO ↔ Customer (anti-echo strongest)
- Pass 2: same-provider with strict model uniqueness (usedModels Set)
- Pass 3: refuse if PO.modelId === Customer.modelId
- Refuse if any single configured provider has ≤5 models AND no second provider key configured

Look at existing model/provider config:
- `src/config/` for provider key store
- search `routeModel` for EE override hook (if absent, document as TODO and proceed with cold-start only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: role-registry.ts with deterministic resolution + refusal cases</name>
  <files>src/product-loop/role-registry.ts, src/product-loop/__tests__/role-registry.test.ts</files>
  <behavior>
    Tests must cover:
    - 2-provider setup (e.g., openai + anthropic) → PO + Customer get DIFFERENT providers
    - 1-provider setup with 6+ models → PO + Customer get SAME provider but DIFFERENT models
    - 1-provider setup with exactly 5 models → resolve refuses (returns RoleResolutionError)
    - 1-provider setup with 6 models all same tier → 5 distinct models assigned + Customer = 6th model
    - All 6 RoleSlots assigned exactly once; usedModels.size === 6
    - Idempotent: same input → same assignment (deterministic ordering)
    - Tier preference honored when ample models available (PO picks premium first if available)
  </behavior>
  <action>
    1. Discover the actual provider/model inventory source. Look in `src/config/providers.ts`,
       `src/usage/router.ts`, or `src/llm/registry.ts`. If a `listAvailableModels(): { provider, model, tier }[]`
       helper exists, reuse it. If not, define an injected `inventory` parameter to keep this module pure
       and testable.
    2. Implement `resolveRoles({ inventory, eeRouteOverride? }): RoleResolutionResult` returning either
       `{ kind: "ok", roles, sameProvider }` or `{ kind: "refuse", reason: "single_provider_too_few" | "po_customer_collision" | "no_inventory" }`.
    3. Use a stable sort by (tier-preference-rank, provider, model) to make tests deterministic.
    4. Apply EE override LAST: if `eeRouteOverride(slot)` returns a model not in inventory, ignore.
    5. Export shapes: `ModelAssignment { slot, provider, model, tier, source: "ee" | "cold-start" }`.
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/role-registry.test.ts</automated>
  </verify>
  <done>
    - All 7+ test cases green
    - Refusal returns structured error (not throw) so loop-driver can format the user message
    - PO.modelId !== Customer.modelId is invariant across all OK results
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: role-memory.ts with 2KB cap + oldest-first truncation</name>
  <files>src/product-loop/role-memory.ts, src/product-loop/__tests__/role-memory.test.ts</files>
  <behavior>
    - appendRoleMemory(flowDir, runId, slot, sprintN, content) writes to runs/<id>/memory/<slot>.md
    - File seeded with empty "Memory" section on first append
    - Content stored as append-only blocks: `### Sprint <N>\n<content>\n`
    - When total file size > 2048 bytes, drop oldest sprint blocks until ≤ 2048 bytes
    - readRoleMemory(flowDir, runId, slot) returns full text (capped) or "" when missing
    - Atomic writes via writeArtifact (not raw fs)
    - Concurrent appends to different slots do not collide (different file paths — no cross-slot lock needed)
  </behavior>
  <action>
    1. Tests first:
       - Append 1 small block → file < 2KB → readback equals input
       - Append 50 blocks of 100 bytes each → file capped at ~2KB, sprint 1 evicted before sprint 50
       - Truncation preserves block boundaries (never mid-block)
       - Missing slot returns "" not throws
    2. Implementation: read existing memory file via fs.readFile (not readArtifact — this file lives
       under runs/<id>/memory/<slot>.md, not the run root). Use atomicWriteText from `src/storage/atomic-io.ts`.
    3. Path helper: `roleMemoryPath(flowDir, runId, slot) -> path.join(flowDir, "runs", runId, "memory", `${slot}.md`)`.
    4. Ensure `memory/` directory exists with `fs.mkdir({ recursive: true })` on first append.
  </action>
  <verify>
    <automated>npx vitest run src/product-loop/__tests__/role-memory.test.ts</automated>
  </verify>
  <done>
    - 2KB cap holds under 50-block append stress test
    - Oldest sprint evicted first (verify by reading post-truncation content does NOT contain "Sprint 1")
    - Cross-slot writes do not interfere (parallel test with PO + Customer + Tester each appending)
  </done>
</task>

</tasks>

<verification>
```
npx tsc --noEmit
npx vitest run src/product-loop/__tests__/role-registry.test.ts src/product-loop/__tests__/role-memory.test.ts
```
</verification>

<success_criteria>
- ROADMAP success criterion #4: "PO and Customer slots resolve to distinct models; same-model is a hard refuse" — DELIVERED
- R2 mitigation: refuse start with single-provider ≤5 models — DELIVERED
- R6 mitigation: 2KB cap with oldest-first truncation — DELIVERED
</success_criteria>

<output>
After completion: 13-02-SUMMARY.md listing the resolveRoles signature + memory invariants for downstream
plans.
</output>
