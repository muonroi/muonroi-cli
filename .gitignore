# Bun
node_modules/
bun.lockb
.bun/

# TypeScript
dist/
*.tsbuildinfo

# Atomic IO leftovers (safety — should not land in repo)
*.tmp
*.tmp.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# CI artifacts
deps-outdated.txt

# Logs
*.log

# Dev-time muonroi-cli artifacts
# (live in user home, but if MUONROI_CLI_HOME=. is set during dev these may appear here)
.muonroi-cli/
.muonroi-flow/
/.claude/*
!/.claude/settings.json
!/.claude/hooks/
/.claude/hooks/*
!/.claude/hooks/*.cjs
!/.claude/hooks/*.mjs
!/.claude/hooks/*.js

# Debug exports from /export TUI command — local diagnostic only
chat-export-*.txt

# EE ingestion state cache — regenerated on each `ee:ingest-bb` run
.ee-ingest-state.json

# Throwaway scratch space (smoke tests, manual scaffold output)
.scratch/

# PIL Phase G snapshots — manual debug captures, not versioned
docs/phase-g-snapshots/
.npmrc

# npm lockfile (this repo uses bun; package-lock.json appears from accidental npm install)
package-lock.json

# Local debug / one-off artifacts (mcp server state, council debug dumps, brain channel tests)
.mcp.json
council.json
.brain-*.tmp

# Opt-in drive-harness extra cwd roots — machine-specific absolute paths, local dev only
.muonroi-harness-roots.json


# Visual Studio workspace state (IDE-local, never versioned)
.vs/

# Local analytical and scratch files (not versioned)
/analyze_app.ts
/app_original.tsx
/app_original_utf8.tsx
/dummy.js
/dump-session.cjs
/dump-session2.cjs
/extract_*
/find_large_funcs.ts
/fix_chatfeed.ts
/fix_chatfeed_any.ts
/fix_modals.ts
/get-schema.cjs
/message_processor_analysis.txt
/message_processor_original.ts
/mp_head.txt
/nul
/pil_block.ts
/processor_methods.txt
/run_*.txt
/scratch.txt
/seq*.json
/skeleton.txt
/test-stream.ts
/test-stream2.ts
.claude/
