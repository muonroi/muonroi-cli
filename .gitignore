# Bun
node_modules/
bun.lockb
.bun/

# TypeScript
dist/
*.tsbuildinfo

# Atomic IO leftovers (safety — should not land in repo)
*.tmp
*.tmp.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# CI artifacts
deps-outdated.txt

# Logs
*.log

# Dev-time muonroi-cli artifacts
# (live in user home, but if MUONROI_CLI_HOME=. is set during dev these may appear here)
.muonroi-cli/
.muonroi-flow/
/.claude/*
!/.claude/settings.json
!/.claude/hooks/
/.claude/hooks/*
!/.claude/hooks/*.cjs
!/.claude/hooks/*.mjs
!/.claude/hooks/*.js

# Debug exports from /export TUI command — local diagnostic only
chat-export-*.txt

# EE ingestion state cache — regenerated on each `ee:ingest-bb` run
.ee-ingest-state.json

# Throwaway scratch space (smoke tests, manual scaffold output)
.scratch/

# PIL Phase G snapshots — manual debug captures, not versioned
docs/phase-g-snapshots/
.npmrc

